

--------------------------------------------------------------------
Протокол TLS 🌐🔒
--------------------------------------------------------------------
TLS — протокол для безопасной передачи данных в небезопасных сетях. Лекция
рассматривает структуру протокола, его уровни, работу протоколов записи и
оповещений. Лекция структурирована с таблицами и примерами. 😊

Summarize: TLS обеспечивает безопасность через шифрование, MAC и
аутентификацию, используя протоколы записи и оповещений.

--------------------------------------------------------------------
1. Введение: что такое TLS? 🛡️📡
--------------------------------------------------------------------
- **TLS**: семейство протоколов для безопасной передачи данных в
  небезопасных сетях (интернет).
- **Функции безопасности**:
  - Приватность: защита от чтения (шифрование, лекция 2).
  - Целостность: защита от изменений (MAC, лекция 3).
  - Аутентификация: проверка подлинности (PKI, лекция 4).
- **Технологии**:
  - Шифрование (симметричное/асимметричное).
  - Хэш-функции (SHA-256).
  - Цифровая подпись (RSA).
  - Сертификаты (X.509).
  - MAC (код аутентификации сообщений).
- **Проблема**: объединение технологий для безопасности — сложная
  задача, требующая высокой квалификации.
- **Версии протокола**:
  - TLS 1.2: актуальна, широко используется.
  - TLS 1.3: новая, постепенно внедряется.
  - TLS 1.0, 1.1, SSL: устарели (deprecated), небезопасны.

**Пример**: HTTPS использует TLS 1.2 для защиты данных при оплате в
интернет-магазине.

**Таблица: Версии TLS/SSL**:
| Версия   | Год  | Статус         | Безопасность |
|----------|------|----------------|--------------|
| SSL      | 1994–1996 | Устарел        | Нет          |
| TLS 1.0  | 1999 | Устарел        | Нет          |
| TLS 1.1  | 2006 | Устарел        | Нет          |
| TLS 1.2  | 2008 | Актуален       | Да           |
| TLS 1.3  | 2018 | Актуален       | Да           |

Summarize: TLS обеспечивает безопасность через криптографию, TLS 1.2 и
1.3 актуальны, старые версии небезопасны.

--------------------------------------------------------------------
2. Уровни протокола TLS 📊🔍
--------------------------------------------------------------------
- **Место в модели TCP/IP**: транспортный уровень, поверх TCP.
- **Место в модели OSI**: транспортный, сеансовый, представления.
- **Структура TLS**:
  - **Нижний уровень**: протокол записи (Record Protocol).
    - Задаёт формат передачи данных.
    - Реализует шифрование и MAC.
  - **Верхний уровень**: четыре протокола:
    - Установка соединений (Handshake): настройка сессии.
    - Оповещение (Alert): сообщения об ошибках.
    - Смена шифра (Change Cipher Spec): переход к шифрованию.
    - Передача данных (Application Data): передача зашифрованных данных.
- **Прикладные протоколы**: HTTPS, SMTP-S и др. передают данные через
  TLS.

**Пример**: HTTPS (порт 443) использует TLS для защиты HTTP-трафика.

**Таблица: Уровни и протоколы TLS**:
| Уровень       | Протокол                     | Роль                              |
|---------------|------------------------------|-----------------------------------|
| Нижний        | Записи (Record)              | Формат, шифрование, MAC           |
| Верхний       | Установка соединений         | Настройка сессии                  |
| Верхний       | Оповещение                   | Сообщения об ошибках              |
| Верхний       | Смена шифра                  | Переход к шифрованию              |
| Верхний       | Передача данных              | Передача зашифрованных данных     |

Summarize: TLS работает поверх TCP, состоит из протокола записи и
четырёх верхних протоколов.

--------------------------------------------------------------------
3. Протокол записи (Record Protocol) 📜🔐
--------------------------------------------------------------------
- **Роль**: основной протокол TLS, отвечает за:
  - Шифрование данных.
  - Проверку целостности (MAC).
  - Формат передачи данных.
- **Формат сообщения**:
  - **Заголовок** (не шифруется):
    - Тип сообщения: какой верхний протокол (установка, данные и т.д.).
    - Версия протокола: TLS 1.2, 1.3 и т.д.
    - Длина сообщения.
  - **Данные**: шифруются, включают MAC.
- **Уязвимость**:
  - Заголовок не шифруется, злоумышленник видит тип, версию, длину.
  - Длина может раскрывать информацию (зависит от протокола).
  - Решение: дополнять сообщения до фиксированной длины.
- **Процесс**:
  1. Получение данных от верхнего протокола (например, HTTPS).
  2. Разбиение данных на фрагменты.
  3. Вычисление MAC. 
  4. Шифрование данных и MAC (симметричный ключ, AES).
  5. Передача в сегментах TCP.
- **Получение**:
  1. Расшифровка данных (симметричный ключ).
  2. Проверка MAC (целостность).
  3. Запись в буфер, объединение фрагментов.

**Пример**: HTTPS-данные шифруются AES, MAC (SHA-256) проверяет
целостность.

**Таблица: Формат сообщения протокола записи**:
| Поле          | Описание                              | Шифрование |
|---------------|---------------------------------------|------------|
| Тип           | Верхний протокол (Handshake, Data)    | Нет        |
| Версия        | TLS 1.2, 1.3                          | Нет        |
| Длина         | Размер данных                         | Нет        |
| Данные + MAC  | Зашифрованные данные и код MAC        | Да         |

Summarize: Протокол записи шифрует данные, проверяет целостность через
MAC, но заголовок уязвим.

--------------------------------------------------------------------
4. Порядок шифрования и MAC в TLS 1.2 ⚠️🔧
--------------------------------------------------------------------
- **Процесс в TLS 1.2**:
  1. Вычисление MAC (данные + ключ).
  2. Шифрование данных и MAC (симметричный ключ).
  3. Передача.
- **Получение**:
  1. Расшифровка.
  2. Проверка MAC.
- **Проблема**:
  - Нельзя проверить MAC без расшифровки.
  - Если сообщение изменено, ресурсы на расшифровку тратятся зря.
- **Улучшение в TLS 1.3**:
  - Другой порядок (рассмотрят в следующей лекции).
- **Решение**: проверять целостность до расшифровки для оптимизации.

**Пример**: В TLS 1.2 злоумышленник изменяет пакет, сервер тратит
ресурсы на расшифровку.

**Таблица: Порядок в TLS 1.2**:
| Шаг           | Действие                              | Проблема                |
|---------------|---------------------------------------|-------------------------|
| 1             | Вычисление MAC                        | -                       |
| 2             | Шифрование (данные + MAC)             | -                       |
| 3             | Расшифровка на сервере                | Трата ресурсов          |
| 4             | Проверка MAC                          | Поздняя проверка        |

Summarize: TLS 1.2 шифрует после MAC, что неэффективно; TLS 1.3
оптимизирует порядок.

--------------------------------------------------------------------
5. Сессии TLS: установка и возобновление 🔄🔑
--------------------------------------------------------------------
- **Сессия TLS**:
  - Состояние соединения между клиентом и сервером.
  - Включает: набор шифров, ключи (шифрование, MAC).
- **Установка сессии**:
  - Выполняется протоколом установки соединений (Handshake).
  - Клиент и сервер договариваются:
    - Набор шифров (RSA, AES, SHA-256).
    - Разделяемые ключи (Диффи-Хеллмана).
  - Требует много ресурсов и времени.
- **Возобновление сессии**:
  - Если клиент недавно подключался, повторная установка не нужна.
  - Клиент использует идентификатор сессии.
  - Передаёт серверу: "Используем старые ключи и шифры".
  - Плюс: быстрее, меньше нагрузки.
  - Минус: ниже безопасность (ключи не обновляются).
- **Идентификатор сессии**:
  - Уникальный для каждой сессии.
  - Связан с узлом (сервером).

**Пример**:
- Браузер подключается к sazykin.ru (HTTPS).
- Через час повторно использует старую сессию, экономя время.

**Таблица: Сессии TLS**:
| Тип                 | Процесс                               | Плюсы                   | Минусы                  |
|---------------------|---------------------------------------|-------------------------|-------------------------|
| Установка           | Обмен шифрами, ключами                | Высокая безопасность    | Медленно, ресурсоёмко   |
| Возобновление       | Использование старых ключей           | Быстро, экономично      | Меньше безопасности     |

Summarize: Сессии TLS настраивают шифры и ключи, возобновление ускоряет
соединение.

--------------------------------------------------------------------
6. Протокол оповещений (Alert Protocol) 🚨📢
--------------------------------------------------------------------
- **Роль**: передача сообщений об ошибках и предупреждениях.
- **Типы ошибок**:
  - **Фатальные (Fatal)**:
    - Соединение разрывается.
    - Примеры:
      - Ошибка MAC: нарушение целостности.
      - Неизвестный CA: сертификат не проверен.
      - Ошибка расшифровки: данные не читаются.
  - **Предупреждения (Warning)**:
    - Соединение может продолжаться.
    - Пользователь решает, продолжать или нет.
    - Примеры:
      - Истёк срок сертификата: ключ может быть скомпрометирован.
      - Сертификат отозван: закрытый ключ скомпрометирован.
      - Неизвестный формат сертификата: клиент не читает сертификат.
- **Реакция**:
  - Браузер показывает предупреждение (например, "Небезопасный сайт").
  - Пользователь может игнорировать (небезопасно).

**Пример**:
- Браузер видит истёкший сертификат sazykin.ru.
- Показывает предупреждение, пользователь может продолжить.

**Таблица: Ошибки в протоколе оповещений**:
| Тип                 | Пример                                | Последствия             |
|---------------------|---------------------------------------|-------------------------|
| Фатальная           | Ошибка MAC                            | Разрыв соединения       |
| Фатальная           | Неизвестный CA                        | Разрыв соединения       |
| Предупреждение      | Истёк срок сертификата                | Пользователь решает     |
| Предупреждение      | Сертификат отозван                    | Пользователь решает     |

Summarize: Протокол оповещений сообщает об ошибках, фатальные разрывают
соединение, предупреждения — на усмотрение пользователя.

--------------------------------------------------------------------
7. Передача данных в TLS 📡🔒
--------------------------------------------------------------------
- **Процесс**:
  1. Клиент получает данные от прикладного протокола (HTTPS).
  2. Протокол передачи данных разбивает данные на фрагменты.
  3. Для каждого фрагмента:
     - Вычисляется MAC.
     - Данные + MAC шифруются.
  4. Передача через протокол записи в сегментах TCP.
- **Получение**:
  1. Сервер расшифровывает фрагмент.
  2. Проверяет MAC (целостность).
  3. Записывает в буфер.
  4. Объединяет фрагменты, передаёт прикладному протоколу.
- **Особенности**:
  - Шифрование и MAC обеспечивают приватность и целостность.
  - TLS 1.2: MAC перед шифрованием (неэффективно).
  - TLS 1.3: оптимизированный порядок (следующая лекция).

**Пример**:
- Браузер отправляет запрос HTTPS.
- Данные шифруются AES, MAC проверяет целостность на сервере.

**Таблица: Передача данных в TLS**:
| Шаг                 | Действие                              | Протокол                |
|---------------------|---------------------------------------|-------------------------|
| 1                   | Разбиение данных                      | Передача данных         |
| 2                   | Вычисление MAC                        | Записи                  |
| 3                   | Шифрование (данные + MAC)             | Записи                  |
| 4                   | Передача в TCP                        | Записи                  |
| 5                   | Расшифровка и проверка MAC            | Записи                  |
| 6                   | Объединение фрагментов                | Передача данных         |

Summarize: TLS разбивает, шифрует и проверяет данные, передавая через
TCP.

--------------------------------------------------------------------
8. Связь с курсом "Компьютерные сети" 📚🔗
--------------------------------------------------------------------
- **TCP/IP**: TLS работает на транспортном уровне, поверх TCP (лекция
  про TCP/IP).
- **OSI**: TLS охватывает транспортный, сеансовый, представления уровни
  (лекция про OSI).
- **HTTPS**: TLS защищает HTTP-трафик (лекция про прикладные
  протоколы).
- **Шифрование**: TLS использует AES, RSA, SHA-256 (лекция 2).
- **Целостность**: MAC для проверки данных (лекция 3).
- **PKI**: Аутентификация через сертификаты (лекция 4).
- **OSPF**: OSPF маршрутизирует без шифрования, TLS добавляет
  безопасность (лекция про OSPF).

**Пример**:
- В университетской сети OSPF маршрутизирует пакеты.
- TLS защищает HTTPS-доступ к сайту.

**Таблица: Связь с курсом**:
| Тема                | Связь с TLS                           | Пример                  |
|---------------------|---------------------------------------|-------------------------|
| TCP/IP              | TLS поверх TCP                        | HTTPS на порт 443       |
| Шифрование          | AES, RSA, Диффи-Хеллмана              | Шифрование HTTPS        |
| PKI                 | Сертификаты X.509                     | Аутентификация сервера  |
| OSPF                | OSPF без шифрования, TLS защищает     | Маршруты + HTTPS        |

Summarize: TLS связан с TCP/IP, HTTPS, криптографией, дополняет OSPF.

--------------------------------------------------------------------
9. Итог: протокол TLS 🎯🔥
--------------------------------------------------------------------
- **TLS**: обеспечивает безопасность (приватность, целостность,
  аутентификацию).
- **Уровни**:
  - Протокол записи: шифрование, MAC, формат данных.
  - Верхние протоколы: установка, оповещение, смена шифра, данные.
- **Сессии**:
  - Установка: выбор шифров, обмен ключами.
  - Возобновление: ускорение за счёт старых ключей.
- **Протокол записи**:
  - Шифрует данные и MAC, но заголовок уязвим.
  - TLS 1.2: MAC перед шифрованием (неэффективно).
- **Оповещения**:
  - Фатальные ошибки: разрыв соединения.
  - Предупреждения: решение пользователя.
- **Версии**: TLS 1.2 и 1.3 актуальны, старые — небезопасны.

**Пример**:
- HTTPS-сайт использует TLS 1.2.
- Данные шифруются AES, MAC проверяет целостность, сертификат
  аутентифицирует сервер.

**Таблица: Ключевые элементы TLS**:
| Элемент             | Роль                                  | Пример                  |
|---------------------|---------------------------------------|-------------------------|
| Протокол записи     | Шифрование, MAC, формат               | AES, SHA-256            |
| Сессия              | Настройка шифров и ключей             | TLS_DHE_RSA_...         |
| Оповещения          | Сообщения об ошибках                  | Истёкший сертификат     |
| Версия              | TLS 1.2, 1.3                          | HTTPS-соединение        |

Summarize: TLS защищает данные через шифрование, MAC и аутентификацию,
оптимизирован в TLS 1.3.