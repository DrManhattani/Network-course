
--------------------------------------------------------------------
Введение в протоколы TLS и SSL 🌐🔒
--------------------------------------------------------------------

--------------------------------------------------------------------
1. Что такое TLS и SSL? 🛡️📡
--------------------------------------------------------------------
- **TLS** (Transport Layer Security): протокол защиты транспортного
  уровня, современный стандарт.
- **SSL** (Secure Socket Layer): предшественник TLS, уровень защищённых
  сокетов, устарел.
- **Идея**: создать безопасные сокеты (как Unix/Berkeley), защищающие
  данные в интернете.
- **Цель**: обеспечить безопасность в небезопасных сетях, где
  злоумышленники могут перехватывать/менять данные.

**Таблица: Основные характеристики**:
| Протокол | Расшифровка                | Статус              |
|----------|----------------------------|---------------------|
| TLS      | Transport Layer Security   | Актуален (1.2, 1.3) |
| SSL      | Secure Socket Layer        | Устарел             |

**Пример**: HTTPS (защищённый HTTP) использует TLS для шифрования.

Summarize: TLS — современный протокол, SSL — устаревший предшественник,
оба для безопасности данных.

--------------------------------------------------------------------
2. Где работают TLS/SSL? 📍🌐
--------------------------------------------------------------------
- **Уровень в модели TCP/IP**: транспортный, поверх TCP.
- **Уровни в модели OSI**: транспортный, сеансовый, представления.
  - Транспортный: передача данных между хостами.
  - Представления: шифрование данных.
  - Сеансовый: управление сессиями (повторное использование ключей).
- **Прикладные протоколы**:
  - HTTPS (HTTP + TLS, порт 443).
  - SMTP-S, POP3-S, IMAP-S ("S" — Secure, используют TLS).

**Таблица: Применение TLS**:
| Протокол | Порт | Описание                     | TLS? |
|----------|------|------------------------------|------|
| HTTP     | 80   | Передача гипертекста         | Нет  |
| HTTPS    | 443  | Защищённый HTTP              | Да   |
| SMTP     | 25   | Электронная почта            | Нет  |
| SMTP-S   | 25   | Защищённая почта (StartTLS)  | Да   |

**Пример**: Браузер показывает замок для HTTPS (порт 443, TLS).

Summarize: TLS работает поверх TCP, используется в HTTPS, SMTP-S и др.,
на уровнях транспортного, сеансового, представления.

--------------------------------------------------------------------
3. Функции безопасности TLS/SSL 🔐🛡️
--------------------------------------------------------------------
TLS/SSL обеспечивают 3 аспекта безопасности:
1. **Приватность (Privacy)**:
   - Защита от пассивных атак (перехват данных).
   - Метод: шифрование данных.
   - Пример: Злоумышленник не может прочитать номер карты в HTTPS.
2. **Целостность (Integrity)**:
   - Защита от активных атак (изменение данных).
   - Метод: криптографические хэш-функции.
   - Пример: Изменение суммы платежа в банке будет обнаружено.
3. **Аутентификация (Authentication)**:
   - Подтверждение, что сервер — тот, за кого себя выдаёт.
   - Методы: цифровая подпись, PKI (инфраструктура открытых ключей).
   - Пример: Защита от атаки "человек посередине" (Man-in-the-Middle).

**Таблица: Функции безопасности**:
| Функция        | Атака                | Защита                     |
|----------------|----------------------|----------------------------|
| Приватность    | Пассивная (перехват) | Шифрование                 |
| Целостность    | Активная (изменение) | Хэш-функции                |
| Аутентификация | Man-in-the-Middle    | Цифровая подпись, PKI      |

**Пример**: При оплате в банке TLS шифрует данные (приватность),
проверяет целостность (хэш) и подтверждает, что это банк (PKI).

Summarize: TLS/SSL защищают от перехвата, изменения и подделки через
шифрование, хэш-функции и PKI.

--------------------------------------------------------------------
4. История развития TLS/SSL 📜⏳
--------------------------------------------------------------------
- **1994**: Netscape начинает разработку SSL для безопасных транзакций
  (например, с кредитными картами).
- **SSL 1.0**: Не опубликован из-за уязвимостей.
- **1995**: SSL 2.0, уязвимости найдены быстро.
- **1996**: SSL 3.0, стандарт RFC 6101.
- **1999**: IETF переименовывает в TLS, выпускает TLS 1.0 (улучшенный
  SSL 3.0).
- **2006**: TLS 1.1.
- **2008**: TLS 1.2 (популярна сегодня).
- **2018**: TLS 1.3 (новая, но уже используется).

**Таблица: Версии протоколов**:
| Версия   | Год  | Статус        | Безопасность |
|----------|------|---------------|--------------|
| SSL 1.0  | 1994 | Не опубликован| Нет          |
| SSL 2.0  | 1995 | Устарел       | Нет          |
| SSL 3.0  | 1996 | Устарел       | Нет          |
| TLS 1.0  | 1999 | Устарел       | Нет          |
| TLS 1.1  | 2006 | Устарел       | Нет          |
| TLS 1.2  | 2008 | Актуален      | Да           |
| TLS 1.3  | 2018 | Актуален      | Да           |

**Примечание**: Только TLS 1.2 и 1.3 безопасны. SSL и TLS < 1.2
уязвимы, использовать нельзя.

Summarize: SSL (1994–1996) устарел, TLS (1999–2018) развивается, только
1.2 и 1.3 безопасны.

--------------------------------------------------------------------
5. Как TLS/SSL интегрируются с протоколами? 🔌🌍
--------------------------------------------------------------------
- **Вариант 1: Выделенный порт**:
  - HTTP: порт 80 (незащищённый).
  - HTTPS: порт 443 (TLS).
  - Пример: Подключение к https://bank.ru идёт через порт 443.
- **Вариант 2: StartTLS**:
  - Сервер на обычном порту (например, SMTP на 25).
  - Клиент подключается, затем вызывает StartTLS для шифрования.
  - Пример: SMTP-S начинает незащищённо, затем переходит на TLS.

**Таблица: Интеграция TLS**:
| Протокол | Порт | Метод TLS         | Пример                 |
|----------|------|-------------------|------------------------|
| HTTPS    | 443  | Выделенный порт   | Сайт банка             |
| SMTP-S   | 25   | StartTLS          | Защищённая почта       |

**Пример**: В браузере замок у сайта — TLS на порту 443. В почте
StartTLS защищает SMTP.

Summarize: TLS интегрируется через выделенный порт (HTTPS) или
StartTLS (SMTP-S).

--------------------------------------------------------------------
6. Технические особенности TLS/SSL 🛠️🔍
--------------------------------------------------------------------
- **Зависимость от TCP**: TLS работает поверх TCP для надёжной доставки.
- **Шифрование**: Требует ресурсов и времени для установки соединения.
  - Решение: повторное использование ключей в сессиях.
- **Технологии**:
  - Шифрование (приватность).
  - Криптографические хэш-функции (целостность).
  - Цифровая подпись и PKI (аутентификация).
- **Библиотеки**: OpenSSL, LibreSSL (название "SSL" сохраняется, но
  реализуют TLS).

**Пример**: OpenSSL используется для TLS 1.2 в HTTPS, шифруя данные и
проверяя подпись сервера.

Summarize: TLS использует TCP, шифрование, хэш-функции, PKI; оптимизирует
сессии.

--------------------------------------------------------------------
7. Связь с курсом "Компьютерные сети" 📚🔗
--------------------------------------------------------------------
- **TCP/IP**: TLS работает на транспортном уровне, поверх TCP (лекция
  про TCP/IP).
- **OSI**: TLS охватывает транспортный, сеансовый, представления уровни
  (лекция про OSI).
- **HTTP**: HTTPS — защищённая версия HTTP (лекция про прикладные
  протоколы).
- **Безопасность**: TLS защищает от атак, в отличие от HTTP, SMTP
  (лекция про безопасность).
- **Сравнение с OSPF**: OSPF (маршрутизация) не шифрует данные, TLS
  защищает передачу (лекция про OSPF).

**Пример**: В университетской сети HTTPS (TLS) защищает доступ к сайту,
OSPF маршрутизирует пакеты.

Summarize: TLS связан с TCP/IP, OSI, HTTP, безопасностью, дополняет
протоколы вроде OSPF.

--------------------------------------------------------------------
8. Итог: ключевые моменты TLS/SSL 🎯🔥
--------------------------------------------------------------------
- **TLS/SSL**: протоколы для безопасной передачи данных.
- **Функции**: приватность (шифрование), целостность (хэш-функции),
  аутентификация (PKI).
- **Применение**: HTTPS (порт 443), SMTP-S (StartTLS).
- **История**: SSL (1994–1996, устарел), TLS 1.2–1.3 (2008–2018,
  актуальны).
- **Техника**: поверх TCP, используют шифрование, хэш, подписи.
- **Безопасность**: только TLS 1.2 и 1.3.

**Пример**: При оплате в интернете TLS 1.2 шифрует данные, проверяет
целостность и банк.

Summarize: TLS/SSL — стандарты безопасности для интернета, актуальны
версии 1.2 и 1.3.