

--------------------------------------------------------------------
Анализ протокола TLS 1.3 в Wireshark 🌐🔍
--------------------------------------------------------------------
Лекция показывает, как анализировать TLS 1.3 в Wireshark: настройка
браузера, фильтры, Handshake и расшифровка с ключами. 😊

Summarize: Wireshark анализирует TLS 1.3, показывая Handshake,
сертификаты, HTTP и CloseNotify с ключами.

--------------------------------------------------------------------
1. Введение: зачем анализировать TLS 1.3? 🛡️📡
--------------------------------------------------------------------
- **TLS 1.3**: современный протокол (RFC 8446, 2018), внедряется.
- **Проблема**: не все браузеры используют TLS 1.3 по умолчанию.
- **Цель**: изучить TLS 1.3 в Wireshark, включая Handshake и HTTP.
- **Инструменты**:
  - Wireshark: захват и анализ пакетов.
  - Chrome: браузер с настройкой TLS 1.3 и сохранением ключей.
- **Процесс**:
  1. Настроить Chrome для TLS 1.3 и ключей.
  2. Установить фильтры в Wireshark.
  3. Открыть сайт (sazykin.ru) и захватить трафик.
  4. Расшифровать данные с ключами.

**Пример**: Анализ HTTPS-трафика sazykin.ru в Wireshark с TLS 1.3.

**Таблица: Цели анализа**:
| Аспект            | Описание                          | Инструмент                |
|-------------------|-----------------------------------|---------------------------|
| Протокол          | TLS 1.3                           | Wireshark                 |
| Браузер           | Настройка TLS 1.3                 | Chrome                    |
| Сайт              | Тестовый HTTPS                    | sazykin.ru                |
| Ключи             | Расшифровка                       | Файл ключей               |

Summarize: Wireshark анализирует TLS 1.3 с настройкой Chrome и ключами.

--------------------------------------------------------------------
2. Настройка Chrome для TLS 1.3 и ключей 🖥️🔑
--------------------------------------------------------------------
- **Цель**: заставить Chrome использовать TLS 1.3 и сохранять ключи.
- **Способ**: ярлык Chrome с параметрами.
  - Параметры:
    - `--ssl-version-max=tls1.3`: принудительно TLS 1.3.
    - `--ssl-key-log-file=<путь_к_файлу>`: сохранение ключей.
  - Пример: `chrome --ssl-version-max=tls1.3 --ssl-key-log-file=C:\keys.log`.
- **Процесс**:
  1. Создать ярлык Chrome с параметрами.
  2. Запустить браузер через ярлык.
  3. Открыть сайт (sazykin.ru).
  4. Проверить файл ключей (`C:\keys.log`).
- **Формат файла ключей**:
  - `CLIENT_RANDOM <ClientRandom> <Premaster Secret>`.
- **Безопасность**:
  - Удалить ярлык после экспериментов.
  - Удалить файл ключей.

**Пример**: Ярлык Chrome создаёт `C:\keys.log` для TLS 1.3 на sazykin.ru.

**Таблица: Параметры Chrome**:
| Параметр                  | Значение                  | Роль                      |
|---------------------------|---------------------------|---------------------------|
| `--ssl-version-max`       | `tls1.3`                  | Принудительный TLS 1.3    |
| `--ssl-key-log-file`      | `C:\keys.log`             | Сохранение ключей         |

Summarize: Ярлык Chrome обеспечивает TLS 1.3 и сохраняет ключи.

--------------------------------------------------------------------
3. Настройка Wireshark для анализа TLS 1.3 📡🔧
--------------------------------------------------------------------
- **Фильтры**:
  - Протокол: `tls`.
  - IP-адрес: IP сайта (например, sazykin.ru).
  - Пример: `tls && ip.addr == <IP_sazykin.ru>`.
- **Ключи**:
  - Путь: `Редактирование → Параметры → Протоколы → TLS`.
  - Поле: `Pre-Master-Secret log filename`.
  - Указать: `C:\keys.log`.
- **Процесс**:
  1. Установить фильтры.
  2. Подключить файл ключей.
  3. Запустить захват пакетов.
  4. Открыть сайт в Chrome.
- **Результат**:
  - Без ключей: видны только зашифрованные данные.
  - С ключами: расшифровка Handshake, HTTP, CloseNotify.

**Пример**: Wireshark с фильтром `tls` и `C:\keys.log` для sazykin.ru.

**Таблица: Настройка Wireshark**:
| Параметр          | Значение                          | Роль                      |
|-------------------|-----------------------------------|---------------------------|
| Фильтр            | `tls && ip.addr == IP`            | Ограничение трафика       |
| Pre-Master-Secret | `C:\keys.log`                     | Расшифровка TLS           |

Summarize: Wireshark с фильтрами и ключами расшифровывает TLS 1.3.

--------------------------------------------------------------------
4. Анализ Handshake TLS 1.3 в Wireshark 📜🔄
--------------------------------------------------------------------
- **Наблюдения**:
  - Два соединения для ускорения.
  - TLS 1.3: упрощённый Handshake, шифрование сообщений.
- **Процесс (неудачный выбор шифров)**:
  1. **ClientHello (первое)**:
     - Тип: Handshake.
     - ClientRandom: для ключей.
     - Шифры: 16 AEAD (AES-GCM, ChaCha20-Poly1305).
     - Расширения:
       - Key Share: параметры ECDHE (кривая X25519).
       - Supported Versions: TLS 1.0–1.3.
       - Server Name (SNI): sazykin.ru.
  2. **ServerHello + ChangeCipherSpec**:
     - HelloRetryRequest:
       - Шифр: AES_256_GCM_SHA384.
       - Key Share: кривая secp384r1 (вместо X25519).
       - Алгоритм: ECDHE (по умолчанию).
     - ChangeCipherSpec: начало шифрования.
  3. **ClientHello (повторное) + ChangeCipherSpec**:
     - ClientRandom: тот же.
     - Шифры: те же.
     - Key Share: secp384r1 (по запросу сервера).
     - ChangeCipherSpec: клиент готов шифровать.
  4. **ServerHello + Application Data**:
     - ServerHello:
       - Шифр: AES_256_GCM_SHA384.
       - Key Share: параметры ECDHE (secp384r1).
     - Application Data: зашифрованные сообщения (расшифровка ниже).
- **Отличия от TLS 1.2**:
  - TLS 1.3: шифрует сертификаты и расширения.
  - TLS 1.3: клиент предлагает ключи в первом ClientHello.
  - TLS 1.3: меньше сообщений (2 при удачном выборе шифров).

**Пример**: ClientHello для sazykin.ru предлагает X25519, сервер требует secp384r1.

**Таблица: Сообщения Handshake TLS 1.3**:
| Сообщение          | Содержимое                        | Роль                      |
|--------------------|-----------------------------------|---------------------------|
| ClientHello        | Шифры, ClientRandom, Key Share    | Предложение параметров    |
| HelloRetryRequest  | Шифр, Key Share (secp384r1)       | Запрос нового ClientHello |
| ServerHello        | Шифр, Key Share                   | Выбор параметров          |
| ChangeCipherSpec   | Готовность к шифрованию           | Переход к шифрованию      |

Summarize: TLS 1.3 Handshake включает Key Share, шифрует сообщения.

--------------------------------------------------------------------
5. Расшифровка сообщений TLS 1.3 с ключами 🔓📜
--------------------------------------------------------------------
- **Без ключей**:
  - Application Data: нечитаемые данные.
- **С ключами** (`C:\keys.log`):
  - Расшифрованные сообщения в ServerHello:
    1. **Encrypted Extensions**:
       - Пустое расширение Server Name.
    2. **Certificate**:
       - Два сертификата:
         - sazykin.ru (Let's Encrypt): серийный номер, срок, открытый ключ.
         - Let's Encrypt: подписан корневым CA (DST RSA).
       - Цепочка доверия: клиент проверяет до корневого CA (в ОС).
    3. **Certificate Verify**:
       - Подпись сервера для проверки сертификата.
    4. **Finished**:
       - Зашифрованное подтверждение (AES_256_GCM_SHA384).
  - Клиент отправляет **Finished**:
    - Зашифрованное подтверждение.
- **Ключи**:
  - Генерируются из ClientRandom, Key Share (ECDHE, secp384r1).
  - Используются для AES_256_GCM_SHA384.

**Пример**: Wireshark расшифровывает сертификаты sazykin.ru с ключами.

**Таблица: Расшифрованные сообщения**:
| Сообщение          | Содержимое                        | Роль                      |
|--------------------|-----------------------------------|---------------------------|
| Encrypted Extensions | Пустое Server Name              | Расширения                |
| Certificate        | sazykin.ru, Let's Encrypt         | Аутентификация            |
| Certificate Verify | Подпись сертификата               | Проверка подлинности      |
| Finished           | Зашифрованное подтверждение       | Завершение Handshake      |

Summarize: Ключи позволяют расшифровать сертификаты и Finished.

--------------------------------------------------------------------
6. Передача данных: Application Data 📦🔓
--------------------------------------------------------------------
- **Роль**: HTTP-трафик внутри TLS 1.3.
- **Record Protocol**:
  - Content Type: Application Data.
  - Данные: зашифрованы (AES_256_GCM_SHA384).
- **С ключами**:
  - Расшифровка:
    - Запрос: `GET / HTTP/1.1` (sazykin.ru).
    - Ответ: HTTP-ответ сервера.
  - Структура: HTTP → TLS (Application Data) → TCP → IP.
- **Без ключей**:
  - Нечитаемые данные.

**Пример**: Wireshark показывает `GET /` для sazykin.ru с ключами.

**Таблица: Application Data**:
| Состояние         | Вид в Wireshark                   | Содержимое                |
|-------------------|-----------------------------------|---------------------------|
| С ключами         | HTTP (GET)                        | Расшифрованный HTTP       |
| Без ключей        | Application Data                  | Нечитаемые данные         |

Summarize: Application Data содержит расшифрованный HTTP с ключами.

--------------------------------------------------------------------
7. Разрыв соединения: CloseNotify 🚪🔐
--------------------------------------------------------------------
- **Протокол**: Alert Protocol.
- **Сообщение**: CloseNotify.
- **Роль**: Клиент предлагает разрыв после получения HTTP-ответа.
- **С ключами**:
  - Wireshark показывает CloseNotify.
- **Без ключей**:
  - Зашифрованное уведомление.

**Пример**: CloseNotify завершает соединение sazykin.ru.

**Таблица: Разрыв соединения**:
| Состояние         | Сообщение                         | Вид в Wireshark           |
|-------------------|-----------------------------------|---------------------------|
| С ключами         | CloseNotify                       | Разрыв соединения         |
| Без ключей        | Encrypted Alert                   | Нечитаемое уведомление    |

Summarize: CloseNotify завершает TLS 1.3 соединение.

--------------------------------------------------------------------
8. Сравнение TLS 1.3 и TLS 1.2 📊🔄
--------------------------------------------------------------------
- **Handshake**:
  - TLS 1.2: больше сообщений, сертификаты открыты.
  - TLS 1.3: меньше сообщений, шифрование сертификатов, Key Share в ClientHello.
- **Шифры**:
  - TLS 1.2: AES, RC4, SHA-1.
  - TLS 1.3: только AEAD (AES-GCM, ChaCha20-Poly1305).
- **Безопасность**:
  - TLS 1.3: обязательная прямая секретность (ECDHE).
- **Производительность**:
  - TLS 1.3: быстрее за счёт упрощённого Handshake.

**Пример**: TLS 1.3 быстрее и безопаснее для sazykin.ru.

**Таблица: TLS 1.2 vs TLS 1.3**:
| Аспект            | TLS 1.2                   | TLS 1.3                   |
|-------------------|---------------------------|---------------------------|
| Шифры             | AES, RC4                  | AES-GCM, ChaCha20         |
| Handshake         | 4+ сообщений              | 2–3 сообщения             |
| Сертификаты       | Открыты                   | Зашифрованы               |
| Key Share         | После ServerHello         | В ClientHello             |

Summarize: TLS 1.3 шифрует сертификаты, упрощает Handshake.

--------------------------------------------------------------------
9. Безопасность: защита ключей 🔒⚠️
--------------------------------------------------------------------
- **Риски**:
  - Утечка файла ключей → расшифровка трафика.
- **Рекомендации**:
  - Удалить ярлык Chrome после экспериментов.
  - Удалить `C:\keys.log`.
  - Ограничить доступ к файлу ключей.

**Пример**: Удаление `C:\keys.log` защищает ключи sazykin.ru.

**Таблица: Меры безопасности**:
| Действие                  | Цель                              | Риск без действия         |
|---------------------------|-----------------------------------|---------------------------|
| Удалить ярлык             | Прекратить запись ключей          | Постоянная запись         |
| Удалить файл ключей       | Защита ключей                     | Утечка ключей             |

Summarize: Удаление ярлыка и файла ключей защищает от утечки.

--------------------------------------------------------------------
10. Связь с курсом "Компьютерные сети" 📚🔗
--------------------------------------------------------------------
- **TCP/IP**: TLS 1.3 работает поверх TCP (лекция про TCP/IP).
- **HTTP**: TLS 1.3 защищает HTTP (лекция про прикладные протоколы).
- **Шифрование**: ECDHE, AES-GCM (лекция 2).
- **Целостность**: SHA384 в AEAD (лекция 3).
- **PKI**: Сертификаты X.509, Let's Encrypt (лекция 4).
- **OSPF**: OSPF маршрутизирует без шифрования, TLS защищает (лекция про OSPF).

**Пример**:
- OSPF маршрутизирует пакеты.
- TLS 1.3 защищает HTTP для sazykin.ru.

**Таблица: Связь с курсом**:
| Тема              | Связь с TLS 1.3                   | Пример                  |
|-------------------|-----------------------------------|-------------------------|
| TCP/IP            | TLS поверх TCP                    | HTTPS на порт 443       |
| HTTP              | TLS защищает HTTP                 | GET / sazykin.ru        |
| Шифрование        | ECDHE, AES-GCM                    | Шифрование HTTPS        |
| PKI               | Сертификаты X.509                 | Let's Encrypt           |
| OSPF              | OSPF без шифрования, TLS защищает | Маршруты + HTTPS        |

Summarize: TLS 1.3 связан с TCP/IP, HTTP, криптографией, дополняет OSPF.

--------------------------------------------------------------------
11. Итог: анализ TLS 1.3 в Wireshark 🎯🔥
--------------------------------------------------------------------
- **Процесс**:
  - Настроить Chrome: `--ssl-version-max=tls1.3`, `--ssl-key-log-file`.
  - Настроить Wireshark: фильтры `tls`, файл ключей.
  - Захватить трафик sazykin.ru.
- **TLS 1.3**:
  - Handshake: ClientHello с Key Share, HelloRetryRequest, ServerHello.
  - Сертификаты: sazykin.ru, Let's Encrypt (зашифрованы).
  - Данные: HTTP (`GET /`) в Application Data.
  - Разрыв: CloseNotify.
- **Отличия от TLS 1.2**:
  - Шифрование сертификатов.
  - Key Share в ClientHello.
  - Меньше сообщений.
- **Безопасность**:
  - Удалить ярлык и файл ключей.

**Пример**:
- Wireshark расшифровывает `GET /` и CloseNotify для sazykin.ru.

**Таблица: Ключевые элементы TLS 1.3**:
| Элемент           | Роль                              | Пример                  |
|-------------------|-----------------------------------|-------------------------|
| ClientHello       | Шифры, Key Share (X25519)         | sazykin.ru              |
| HelloRetryRequest | Новый шифр (secp384r1)            | AES_256_GCM_SHA384      |
| Certificate       | sazykin.ru, Let's Encrypt         | Аутентификация          |
| Application Data  | HTTP (GET)                        | Расшифрованный запрос   |
| CloseNotify       | Разрыв соединения                 | Завершение сессии       |

Summarize: Wireshark анализирует TLS 1.3, расшифровывая Handshake и HTTP.