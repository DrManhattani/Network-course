

--------------------------------------------------------------------
Протокол TLS 1.3 🌐🔒
--------------------------------------------------------------------
Лекция описывает TLS 1.3: современную версию TLS (2018, RFC 8446),
улучшающую безопасность и производительность по сравнению с TLS 1.2. 😊

Summarize: TLS 1.3 повышает безопасность (AEAD, прямая секретность) и
производительность (упрощённый Handshake, 0-RTT).

--------------------------------------------------------------------
1. Введение в TLS 1.3: зачем нужна новая версия? 🛡️📡
--------------------------------------------------------------------
- **TLS 1.3**: опубликован в 2018 (RFC 8446), внедряется, но не вытеснил TLS 1.2.
- **TLS 1.2**: стандарт с 2008 (RFC 5246), устарел к 2018.
- **Контекст изменений**:
  - 2008: шифрование редко (пароли, кредитки).
  - 2018: 80% трафика Chrome и 70% Firefox шифрованы (Google, Let's Encrypt).
  - Рост мобильных устройств: меньше вычислительных ресурсов, высокие задержки в сетях.
- **Проблемы TLS 1.2**:
  - Низкая производительность: сложный Handshake, много сообщений.
  - Уязвимости: атаки (RFC 7457), устаревшие шифры (RC4, MD5, SHA-1).
  - Обратная совместимость: поддержка небезопасных технологий.
- **Цели TLS 1.3**:
  - Повысить безопасность: убрать устаревшие шифры, обязательная прямая секретность.
  - Увеличить производительность: упростить Handshake, добавить 0-RTT.

**Пример**: HTTPS-сайт в 2018 использует TLS 1.3 для скорости и безопасности.

**Таблица: Контекст TLS 1.2 vs TLS 1.3**:
| Аспект            | TLS 1.2 (2008)            | TLS 1.3 (2018)            |
|-------------------|---------------------------|---------------------------|
| Шифрование        | Редко                     | 70-80% трафика            |
| Устройства        | ПК                        | Мобильные устройства      |
| Проблемы          | Атаки, совместимость      | Задержки, производительность |
| Цели              | Гибкость                  | Безопасность, скорость    |

Summarize: TLS 1.3 решает проблемы TLS 1.2: безопасность и скорость для мобильных сетей.

--------------------------------------------------------------------
2. Улучшения безопасности в TLS 1.3 🔐🔍
--------------------------------------------------------------------
- **Проблема TLS 1.2**: поддержка устаревших технологий (RC4, MD5, SHA-1).
  - Легко выбрать небезопасный шифр или сочетание алгоритмов.
- **Решения TLS 1.3**:
  1. **Запрет устаревших технологий**:
     - Шифрование: RC4, MD5.
     - MAC: SHA-1.
     - Обмен ключами: статический RSA, статический Диффи-Хеллман.
  2. **Шифры AEAD (Authenticated Encryption with Associated Data)**:
     - Комбинируют шифрование и MAC (приватность + целостность).
     - Примеры:
       - AES-GCM (Galois/Counter Mode).
       - ChaCha20-Poly1305 (ChaCha20: шифрование, Poly1305: MAC).
     - Плюс: безопасность гарантирована для алгоритма целиком.
  3. **Обязательная прямая секретность**:
     - Ключи уникальны для каждой сессии.
     - Запрещены RSA и статический Диффи-Хеллман.
     - Используется только ephemeral Диффи-Хеллман (DHE/ECDHE).
     - Злоумышленник с ключом сервера не расшифрует старые данные.
  4. **Отказ от совместимости**:
     - Безопасность важнее поддержки старых устройств.

**Пример**: TLS 1.3 использует AES-GCM и ECDHE для HTTPS на sazykin.ru.

**Таблица: Шифры в TLS 1.2 vs TLS 1.3**:
| Аспект            | TLS 1.2                   | TLS 1.3                   |
|-------------------|---------------------------|---------------------------|
| Шифры             | AES, RC4, MD5             | AES-GCM, ChaCha20-Poly1305 |
| MAC               | SHA-1, SHA-256            | Встроен в AEAD            |
| Обмен ключами     | RSA, DHE, ECDHE           | Только DHE/ECDHE          |
| Прямая секретность | Опционально               | Обязательно               |

Summarize: TLS 1.3 использует AEAD и ECDHE, запрещает устаревшие шифры.

--------------------------------------------------------------------
3. Улучшения производительности в TLS 1.3 ⚡🔄
--------------------------------------------------------------------
- **Проблема TLS 1.2**:
  - Сложный Handshake: много сообщений, задержки на мобильных сетях.
- **Решения TLS 1.3**:
  1. **Упрощённый Handshake**:
     - Меньше сообщений (2 вместо 4 для обмена ключами).
     - Клиент предполагает шифры сервера в ClientHello.
  2. **0-RTT (Zero Round-Trip Time)**:
     - Возобновление сессии с данными в первом сообщении.
     - Клиент отправляет зашифрованные данные (например, GET) в ClientHello.
     - Плюс: ускоряет соединение для мобильных устройств.
     - Минус: снижает безопасность (реплеи-атаки, нет прямой секретности).
- **Результат**:
  - Быстрее установка соединения.
  - Меньше задержек в сетях с высокой латентностью.

**Пример**: TLS 1.3 ускоряет загрузку sazykin.ru на смартфоне.

**Таблица: Производительность TLS 1.2 vs TLS 1.3**:
| Аспект            | TLS 1.2                   | TLS 1.3                   |
|-------------------|---------------------------|---------------------------|
| Handshake         | 4+ сообщений              | 2 сообщения (обычно)      |
| Возобновление     | Session Ticket, ID        | 0-RTT                     |
| Задержки          | Высокие                   | Низкие                    |
| Мобильные сети    | Медленно                  | Быстро                    |

Summarize: TLS 1.3 упрощает Handshake и добавляет 0-RTT для скорости.

--------------------------------------------------------------------
4. Установка соединения в TLS 1.3: Handshake 📜🔄
--------------------------------------------------------------------
- **Отличие от TLS 1.2**:
  - TLS 1.2: клиент отправляет шифры, сервер выбирает, затем обмен ключами.
  - TLS 1.3: клиент предполагает шифры и сразу включает данные для ключей.
- **Процесс (успешный случай)**:
  1. **ClientHello**:
     - Список шифров (AEAD: AES-GCM, ChaCha20-Poly1305).
     - Параметры ECDHE для обмена ключами (yc).
     - ClientRandom, SNI, опционально 0-RTT данные.
  2. **ServerHello**:
     - Выбранный шифр (например, AES-GCM).
     - Параметры ECDHE (ys), ServerRandom.
     - Сертификат сервера, подпись.
     - Сообщение Finished (зашифровано).
  3. **Client Finished**:
     - Клиент подтверждает шифрование (зашифровано).
     - Соединение установлено, передача данных.
- **Ключи**:
  - Генерируются из ClientRandom, ServerRandom, yc, ys (ECDHE).
  - Premaster Secret → ключи для AES-GCM или ChaCha20-Poly1305.
- **Неудачный случай**:
  - Сервер не поддерживает шифры клиента.
  - Сервер отправляет HelloRetryRequest:
    - Указывает поддерживаемый шифр.
    - Включает параметры ECDHE.
  - Клиент повторяет ClientHello с новым шифром и параметрами.
  - Если шифры не совпадают, соединение не устанавливается.

**Пример**: ClientHello для sazykin.ru предлагает AES-GCM, сервер подтверждает.

**Таблица: Сообщения Handshake TLS 1.3**:
| Сообщение          | Содержимое                        | Роль                      |
|--------------------|-----------------------------------|---------------------------|
| ClientHello        | Шифры, yc, ClientRandom, SNI      | Предложение параметров    |
| ServerHello        | Шифр, ys, ServerRandom, сертификат | Выбор параметров          |
| Finished           | Зашифрованное подтверждение       | Завершение Handshake      |
| HelloRetryRequest  | Шифр, параметры ECDHE             | Запрос нового ClientHello |

Summarize: TLS 1.3 упрощает Handshake до 2 сообщений, использует ECDHE.

--------------------------------------------------------------------
5. Аутентификация в TLS 1.3 🔍📜
--------------------------------------------------------------------
- **Аутентификация сервера**:
  - Сервер отправляет сертификат X.509 в ServerHello.
  - Клиент проверяет: подпись CA, домен, срок действия, статус отзыва.
- **Аутентификация клиента** (опционально):
  - Сервер отправляет CertificateRequest после ServerHello.
  - Клиент отправляет сертификат X.509 перед Finished.
  - Сервер проверяет: подпись CA, подлинность, доверие к CA.
  - Используется редко (например, в VPN).
- **Отличие от TLS 1.2**:
  - TLS 1.3: сертификат шифруется (кроме домена).
  - TLS 1.2: сертификат передаётся открыто.

**Пример**: Сервер sazykin.ru подтверждает подлинность сертификатом Let's Encrypt.

**Таблица: Аутентификация в TLS 1.3**:
| Сторона           | Обязательна? | Сообщения                        | Проверка                  |
|-------------------|--------------|----------------------------------|---------------------------|
| Сервер            | Да           | Сертификат в ServerHello         | CA, домен, срок           |
| Клиент            | Нет          | CertificateRequest, сертификат   | CA, подлинность           |

Summarize: TLS 1.3 шифрует сертификаты, поддерживает аутентификацию клиента.

--------------------------------------------------------------------
6. Возобновление сессии: 0-RTT (Zero Round-Trip Time) ⚡🔄
--------------------------------------------------------------------
- **Роль**: Ускорение повторного соединения с данными в первом сообщении.
- **Механизм**:
  - Клиент сохраняет ключи и параметры предыдущей сессии.
  - В ClientHello включает:
    - Тикет сессии (Session Ticket).
    - Зашифрованные данные (Early Data, например, GET-запрос).
  - Сервер проверяет тикет, расшифровывает данные.
  - Отправляет ServerHello, сертификат, Early Data (если есть), Finished.
  - Клиент завершает Handshake сообщением Finished.
- **Пример**:
  - Клиент открывает sazykin.ru, отправляет GET в ClientHello.
  - Сервер отвечает HTML-страницей в ServerHello.
- **Плюсы**:
  - Нет задержек (0-RTT).
  - Идеально для мобильных сетей.
- **Минусы**:
  - Нет прямой секретности (старые ключи).
  - Реплеи-атаки: злоумышленник повторяет ClientHello.
    - Безопасно: GET-запросы.
    - Опасно: команды (перевод денег, удаление данных).
  - RFC 8446: избегать критических операций в 0-RTT.

**Пример**: 0-RTT ускоряет загрузку sazykin.ru, но GET безопаснее перевода денег.

**Таблица: 0-RTT vs Полный Handshake**:
| Аспект            | Полный Handshake          | 0-RTT                     |
|-------------------|---------------------------|---------------------------|
| Сообщения         | ClientHello, ServerHello  | ClientHello с данными     |
| Скорость          | Медленнее                 | Быстрее                   |
| Безопасность      | Прямая секретность        | Реплеи-атаки, старые ключи |
| Данные            | После Handshake           | В ClientHello             |

Summarize: 0-RTT ускоряет сессии, но снижает безопасность.

--------------------------------------------------------------------
7. Сравнение TLS 1.2 и TLS 1.3: ключевые отличия 📊🔄
--------------------------------------------------------------------
- **Безопасность**:
  - TLS 1.2: устаревшие шифры, опциональная прямая секретность.
  - TLS 1.3: только AEAD, обязательная прямая секретность.
- **Производительность**:
  - TLS 1.2: сложный Handshake, много сообщений.
  - TLS 1.3: упрощённый Handshake, 0-RTT.
- **Совместимость**:
  - TLS 1.2: поддержка старых устройств.
  - TLS 1.3: отказ от совместимости ради безопасности.

**Пример**: TLS 1.3 быстрее и безопаснее для HTTPS на sazykin.ru.

**Таблица: TLS 1.2 vs TLS 1.3**:
| Аспект            | TLS 1.2                   | TLS 1.3                   |
|-------------------|---------------------------|---------------------------|
| Шифры             | AES, RC4, MD5             | AES-GCM, ChaCha20-Poly1305 |
| Прямая секретность | Опционально               | Обязательно               |
| Handshake         | 4+ сообщений              | 2 сообщения               |
| Возобновление     | Session Ticket            | 0-RTT                     |
| Совместимость     | Высокая                   | Низкая                    |

Summarize: TLS 1.3 безопаснее и быстрее, но не совместим со старыми устройствами.

--------------------------------------------------------------------
8. Связь с курсом "Компьютерные сети" 📚🔗
--------------------------------------------------------------------
- **TCP/IP**: TLS 1.3 работает поверх TCP (лекция про TCP/IP).
- **HTTP**: TLS 1.3 защищает HTTPS (лекция про прикладные протоколы).
- **Шифрование**: ECDHE, AES-GCM, ChaCha20 (лекция 2).
- **Целостность**: AEAD (MAC встроен) (лекция 3).
- **PKI**: Сертификаты X.509, Let's Encrypt (лекция 4).
- **OSPF**: OSPF маршрутизирует без шифрования, TLS защищает (лекция про OSPF).

**Пример**:
- OSPF маршрутизирует пакеты.
- TLS 1.3 защищает HTTPS-доступ к sazykin.ru.

**Таблица: Связь с курсом**:
| Тема              | Связь с TLS 1.3                   | Пример                  |
|-------------------|-----------------------------------|-------------------------|
| TCP/IP            | TLS поверх TCP                    | HTTPS на порт 443       |
| HTTP              | TLS защищает HTTP                 | GET / sazykin.ru        |
| Шифрование        | ECDHE, AES-GCM                    | Шифрование HTTPS        |
| PKI               | Сертификаты X.509                 | Let's Encrypt           |
| OSPF              | OSPF без шифрования, TLS защищает | Маршруты + HTTPS        |

Summarize: TLS 1.3 связан с TCP/IP, HTTP, криптографией, дополняет OSPF.

--------------------------------------------------------------------
9. Итог: TLS 1.3 в современных сетях 🎯🔥
--------------------------------------------------------------------
- **TLS 1.3**:
  - Опубликован в 2018 (RFC 8446), внедряется.
  - Заменяет TLS 1.2, устаревшие версии (TLS 1.0/1.1, SSL) не рекомендованы.
- **Безопасность**:
  - Шифры AEAD (AES-GCM, ChaCha20-Poly1305).
  - Обязательная прямая секретность (ECDHE).
  - Запрет устаревших технологий (RC4, MD5, SHA-1).
- **Производительность**:
  - Упрощённый Handshake (2 сообщения).
  - 0-RTT для возобновления сессий.
- **Установка соединения**:
  - ClientHello с параметрами ECDHE, ServerHello с сертификатом, Finished.
  - HelloRetryRequest при несовпадении шифров.
- **Аутентификация**:
  - Сервер обязателен, клиент опционален.
  - Шифрованные сертификаты.
- **0-RTT**:
  - Ускоряет, но опасен для критических операций.
- **Пример**:
  - TLS 1.3 ускоряет и защищает HTTPS на sazykin.ru.

**Таблица: Ключевые элементы TLS 1.3**:
| Элемент           | Роль                              | Пример                  |
|-------------------|-----------------------------------|-------------------------|
| Шифры AEAD        | Шифрование и MAC                  | AES-GCM                 |
| ECDHE             | Прямая секретность                | Уникальные ключи        |
| Handshake         | Упрощённая установка              | 2 сообщения             |
| 0-RTT             | Данные в ClientHello              | GET-запрос              |
| Сертификат        | Аутентификация                    | Let's Encrypt           |

Summarize: TLS 1.3 — безопасный и быстрый протокол для современных сетей.