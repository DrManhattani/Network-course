

--------------------------------------------------------------------
Установка соединения в TLS 🌐🔒
--------------------------------------------------------------------
TLS требует установки соединения для выбора шифров, аутентификации и
обмена ключами. Лекция описывает процесс в TLS 1.2 с таблицами и
примерами. 😊

Summarize: Установка соединения в TLS 1.2 согласовывает шифры,
аутентифицирует сервер и обменивается ключами через Handshake.

--------------------------------------------------------------------
1. Зачем нужна установка соединения в TLS? 🛡️📡
--------------------------------------------------------------------
- **TLS**: протокол для безопасной передачи данных в интернете.
- **Цели установки соединения**:
  - Согласование криптографических алгоритмов (набор шифров).
  - Проверка подлинности сервера (иногда клиента).
  - Обмен разделяемыми ключами для шифрования и MAC.
- **Проблема**: разные версии ПО у клиентов и серверов.
  - Новые устройства: TLS 1.3, AES, SHA-256.
  - Старые устройства: TLS 1.0, DES, MD5.
- **Решение**: набор шифров (Cipher Suite) определяет:
  - Обмен ключами: Диффи-Хеллман, RSA.
  - Цифровая подпись: RSA, DSA.
  - Симметричное шифрование: AES, DES.
  - MAC: SHA-256, SHA-384.
- **Гибкость**: новые алгоритмы интегрируются без изменения TLS.
- **Аутентификация**: защита от атак "человек посередине".
  - Проверка сертификата сервера (иногда клиента).
- **Ключи**: для симметричного шифрования (AES) и MAC.

**Пример**: Клиент подключается к банку, выбирает AES_256 и SHA-256,
проверяет сертификат.

**Таблица: Цели установки соединения**:
| Цель                | Описание                              | Пример                  |
|---------------------|---------------------------------------|-------------------------|
| Выбор шифров        | Согласование алгоритмов               | TLS_DHE_RSA_AES_256     |
| Аутентификация      | Проверка сервера/клиента              | Сертификат X.509        |
| Обмен ключами       | Ключи для шифрования и MAC            | Диффи-Хеллман           |

Summarize: Установка соединения выбирает шифры, проверяет подлинность и
обменивается ключами.

--------------------------------------------------------------------
2. Набор шифров (Cipher Suite): что выбирается? 📋🔐
--------------------------------------------------------------------
- **Cipher Suite**: комбинация алгоритмов для TLS-соединения.
- **Компоненты**:
  - Обмен ключами: Диффи-Хеллман (DHE), RSA (лекция 2).
  - Подпись: RSA, DSA (лекция 4).
  - Шифрование: AES, DES, 3DES (лекция 2).
  - MAC: SHA-256, SHA-384 (лекция 3).
- **Гибкость**:
  - Поддержка старых клиентов (DES, RSA).
  - Возможность добавления новых алгоритмов.
- **Пример Cipher Suite**:
  - `TLS_DHE_RSA_WITH_AES_256_CBC_SHA256`:
    - DHE: обмен ключами.
    - RSA: подпись.
    - AES_256_CBC: шифрование.
    - SHA256: MAC.
- **Процесс выбора**:
  - Клиент предлагает список шифров.
  - Сервер выбирает один из предложенных.

**Пример**: Клиент предлагает 10 шифров, сервер выбирает DHE и AES_256.

**Таблица: Пример Cipher Suite**:
| Компонент           | Алгоритм              | Роль                     |
|---------------------|-----------------------|--------------------------|
| Обмен ключами       | Диффи-Хеллман (DHE)   | Передача ключей          |
| Подпись             | RSA                   | Аутентификация           |
| Шифрование          | AES_256_CBC           | Приватность данных       |
| MAC                 | SHA256                | Целостность данных       |

Summarize: Cipher Suite определяет алгоритмы для шифрования, подписи и
MAC.

--------------------------------------------------------------------
3. Процесс установки соединения в TLS 1.2: Handshake 📜🔄
--------------------------------------------------------------------
- **Протоколы**:
  - Handshake Protocol: основная установка соединения.
  - Change Cipher Spec: переход к шифрованию (отдельный в TLS 1.2).
- **Особенности TLS 1.2**:
  - TLS 1.3 упрощает процесс (отдельная лекция).
  - Сообщения могут объединяться в один TCP-сегмент.
- **Процесс** (подробно):

1. **ClientHello**:
   - Клиент отправляет:
     - Список поддерживаемых шифров.
     - Случайное число (ClientRandom) для ключей.
     - Опционально: идентификатор сессии для возобновления.
2. **ServerHello**:
   - Сервер отвечает:
     - Выбранный шифр из списка клиента.
     - Случайное число (ServerRandom).
     - Идентификатор сессии (для возобновления).
3. **Certificate**:
   - Сервер отправляет сертификат X.509:
     - Открытый ключ, домен, подпись CA.
   - Клиент проверяет:
     - Подпись CA (доверие к CA).
     - Домен (совпадает с сервером).
     - Срок действия, статус отзыва.
4. **ServerKeyExchange** (опционально):
   - Для Диффи-Хеллмана (DHE):
     - Параметры: p, g, ys (серверное значение).
   - Не нужно для RSA (ключ в сертификате).
5. **ServerHelloDone**:
   - Сервер завершил первую фазу.
6. **ClientKeyExchange**:
   - Клиент отправляет данные для обмена ключами:
     - RSA: Premaster Secret, зашифрованный открытым ключом.
     - DHE: yc (клиентское значение).
7. **ChangeCipherSpec** (клиент):
   - Клиент готов использовать шифры и ключи.
8. **Finished** (клиент):
   - Зашифрованное сообщение с MAC.
   - Подтверждает целостность и шифрование.
9. **ChangeCipherSpec** (сервер):
   - Сервер готов использовать шифры.
10. **Finished** (сервер):
    - Зашифрованное сообщение с MAC.
    - Подтверждает установку соединения.

**Пример**: Клиент подключается к sazykin.ru, выбирает DHE_AES_256,
проверяет сертификат, обменивается ключами.

**Таблица: Сообщения Handshake в TLS 1.2**:
| Сообщение           | Отправитель | Содержание                            |
|---------------------|-------------|---------------------------------------|
| ClientHello         | Клиент      | Шифры, ClientRandom, ID сессии        |
| ServerHello         | Сервер      | Выбранный шифр, ServerRandom, ID       |
| Certificate         | Сервер      | Сертификат X.509                      |
| ServerKeyExchange   | Сервер      | Параметры DHE (p, g, ys)              |
| ServerHelloDone     | Сервер      | Завершение фазы                       |
| ClientKeyExchange   | Клиент      | Premaster Secret (RSA) или yc (DHE)   |
| ChangeCipherSpec    | Клиент/Сервер | Готовность к шифрованию               |
| Finished            | Клиент/Сервер | Зашифрованное сообщение с MAC         |

Summarize: Handshake в TLS 1.2 выбирает шифры, аутентифицирует и
обменивается ключами.

--------------------------------------------------------------------
4. Обмен ключами: RSA и Диффи-Хеллман 🔑🔄
--------------------------------------------------------------------
- **Обмен ключами**: создание разделяемых ключей для шифрования и MAC.
- **Алгоритмы**:
  - **RSA**:
    - Клиент шифрует Premaster Secret открытым ключом из сертификата.
    - Сервер расшифровывает закрытым ключом.
    - Минус: нет прямой секретности (компрометация ключа раскрывает
      данные).
  - **Диффи-Хеллман (DHE)**:
    - Статический: параметры в сертификате, нет прямой секретности.
    - Ephemeral (DHE): новые параметры (p, g, ys, yc) для каждого
      соединения.
    - Плюс: прямая секретность (компрометация не раскрывает старые
      данные).
- **Процесс**:
  - Клиент и сервер обмениваются данными (Premaster Secret или
    yc/ys).
  - Используют псевдослучайную функцию (PRF):
    - Вход: Premaster Secret, ClientRandom, ServerRandom.
    - Выход: ключи для шифрования и MAC.
- **Защита**:
  - ClientRandom и ServerRandom предотвращают атаки повтора.

**Пример**: DHE генерирует новые ключи для HTTPS, RSA использует
сертификат.

**Таблица: RSA vs Диффи-Хеллман**:
| Алгоритм | Параметры              | Прямая секретность | Ресурсы           |
|----------|------------------------|--------------------|-------------------|
| RSA      | Ключ из сертификата    | Нет                | Меньше            |
| DHE      | p, g, ys, yc           | Да                 | Больше            |

Summarize: DHE обеспечивает прямую секретность, RSA — нет, оба генерируют
ключи.

--------------------------------------------------------------------
5. Аутентификация: проверка сертификата 🔍📜
--------------------------------------------------------------------
- **Аутентификация сервера**:
  - Сервер отправляет сертификат X.509 (лекция 4).
  - Клиент проверяет:
    - Подпись CA (доверие к CA из хранилища ОС).
    - Домен (совпадает с сервером).
    - Срок действия.
    - Статус отзыва.
    - Дополнительные параметры (например, цепочка CA).
- **Аутентификация клиента** (опционально):
  - Сервер запрашивает сертификат клиента.
  - Клиент отправляет сертификат X.509.
  - Сервер проверяет аналогично.
  - Используется редко (не в HTTPS).
- **Результат**: защита от атак "человек посередине".

**Пример**: Клиент проверяет сертификат sazykin.ru, подписанный
Let's Encrypt.

**Таблица: Проверка сертификата**:
| Проверка            | Описание                              | Проблема при сбое       |
|---------------------|---------------------------------------|-------------------------|
| Подпись CA          | Доверенный CA                         | Неизвестный CA          |
| Домен               | Совпадает с сервером                  | Подмена сервера         |
| Срок действия       | Не истёк                              | Скомпрометирован ключ   |
| Статус отзыва       | Не отозван                            | Скомпрометирован ключ   |

Summarize: Аутентификация проверяет сертификат, защищая от подмены
сервера.

--------------------------------------------------------------------
6. Разрыв соединения: CloseNotify 🚪🔐
--------------------------------------------------------------------
- **Протокол**: Alert Protocol (лекция 5).
- **Сообщение**: CloseNotify.
- **Процесс**:
  - Клиент отправляет CloseNotify, считает соединение закрытым.
  - Сервер отвечает CloseNotify, закрывает соединение.
- **Отличие от TCP**:
  - TCP: одна сторона может продолжать отправлять данные.
  - TLS: обе стороны прекращают передачу после CloseNotify.
- **Цель**: безопасное завершение сессии.

**Пример**: Браузер закрывает HTTPS-соединение с банком через
CloseNotify.

**Таблица: Разрыв соединения**:
| Протокол | Сообщение            | Действие                              |
|----------|----------------------|---------------------------------------|
| TLS      | CloseNotify          | Полное закрытие обеими сторонами      |
| TCP      | FIN                  | Одна сторона может продолжать         |

Summarize: CloseNotify завершает TLS-соединение, обе стороны прекращают
передачу.

--------------------------------------------------------------------
7. Возобновление сессии: оптимизация ⚡🔄
--------------------------------------------------------------------
- **Проблема**: Handshake требует времени и ресурсов.
- **Решение**: возобновление сессии.
  - Используется идентификатор сессии из ServerHello.
  - Клиент отправляет ID в ClientHello.
  - Сервер проверяет ID в таблице сессий.
- **Процесс**:
  1. ClientHello с ID сессии.
  2. ServerHello, ChangeCipherSpec, Finished (зашифровано).
  3. Клиент: ChangeCipherSpec, Finished.
- **Плюсы**:
  - Быстрее (без обмена ключами).
  - Меньше ресурсов.
- **Минусы**:
  - Меньше безопасности (старые ключи).

**Пример**: Браузер возобновляет сессию с sazykin.ru, используя старый
ID.

**Таблица: Установка vs Возобновление**:
| Тип                 | Сообщения                             | Плюсы                   |
|---------------------|---------------------------------------|-------------------------|
| Установка           | Полный Handshake                      | Безопасность            |
| Возобновление       | ClientHello, Finished                 | Скорость                |

Summarize: Возобновление сессии ускоряет соединение, но снижает
безопасность.

--------------------------------------------------------------------
8. Аутентификация клиента: редкий случай 🤝🔍
--------------------------------------------------------------------
- **Когда нужна**:
  - Не используется в HTTPS.
  - Применяется в кастомных протоколах (например, VPN).
- **Процесс**:
  - Сервер отправляет CertificateRequest после ServerKeyExchange.
  - Клиент отправляет сертификат X.509.
  - Сервер проверяет сертификат (как клиент проверяет сервер).
- **Требование**:
  - Клиент должен иметь сертификат от доверенного CA.
- **Редкость**: сложность управления клиентскими сертификатами.

**Пример**: VPN требует сертификат клиента для доступа к сети.

**Таблица: Аутентификация сторон**:
| Сторона             | Обязательна? | Пример                        |
|---------------------|--------------|-------------------------------|
| Сервер              | Да           | HTTPS-сайт                    |
| Клиент              | Нет          | VPN с клиентским сертификатом  |

Summarize: Аутентификация клиента опциональна, используется в
специфичных протоколах.

--------------------------------------------------------------------
9. Объединение сообщений: оптимизация 📦🔄
--------------------------------------------------------------------
- **Механизм**:
  - Сообщения Handshake объединяются в один TCP-сегмент через Record
    Protocol.
  - Например:
    - ServerHello, Certificate, ServerKeyExchange, ServerHelloDone.
    - ClientKeyExchange, ChangeCipherSpec, Finished.
- **Плюс**: меньше сетевых операций, быстрее установка.
- **Особенность**: зависит от реализации TLS.

**Пример**: Сервер отправляет 4 сообщения в одном TCP-пакете для
HTTPS.

**Таблица: Объединение сообщений**:
| Сообщения                          | TCP-сегмент | Эффект                |
|------------------------------------|-------------|-----------------------|
| ServerHello, Certificate, ...      | Один        | Меньше задержек       |
| ClientKeyExchange, Finished        | Один        | Быстрее установка     |

Summarize: Объединение сообщений ускоряет Handshake через Record
Protocol.

--------------------------------------------------------------------
10. Связь с курсом "Компьютерные сети" 📚🔗
--------------------------------------------------------------------
- **TCP/IP**: TLS работает поверх TCP (лекция про TCP/IP).
- **HTTPS**: TLS защищает HTTP-трафик (лекция про прикладные
  протоколы).
- **Шифрование**: RSA, DHE, AES (лекция 2).
- **Целостность**: MAC с SHA-256 (лекция 3).
- **PKI**: Сертификаты X.509 (лекция 4).
- **OSPF**: OSPF маршрутизирует без шифрования, TLS добавляет
  безопасность (лекция про OSPF).

**Пример**:
- OSPF маршрутизирует пакеты в сети.
- TLS защищает HTTPS-доступ к сайту.

**Таблица: Связь с курсом**:
| Тема                | Связь с TLS                           | Пример                  |
|---------------------|---------------------------------------|-------------------------|
| TCP/IP              | TLS поверх TCP                        | HTTPS на порт 443       |
| Шифрование          | AES, RSA, DHE                         | Шифрование HTTPS        |
| PKI                 | Сертификаты X.509                     | Аутентификация сервера  |
| OSPF                | OSPF без шифрования, TLS защищает     | Маршруты + HTTPS        |

Summarize: TLS связан с TCP/IP, HTTPS, криптографией, дополняет OSPF.

--------------------------------------------------------------------
11. Итог: установка соединения в TLS 1.2 🎯🔥
--------------------------------------------------------------------
- **Процесс**: Handshake Protocol и Change Cipher Spec.
- **Цели**:
  - Выбор шифров (DHE, RSA, AES, SHA-256).
  - Аутентификация сервера (сертификат X.509).
  - Обмен ключами (Premaster Secret, DHE).
- **Особенности**:
  - TLS 1.2: сложный процесс, оптимизирован в TLS 1.3.
  - Возобновление сессии ускоряет повторные соединения.
  - Аутентификация клиента опциональна.
- **Разрыв**: CloseNotify завершает соединение.
- **Оптимизация**:
  - Объединение сообщений в TCP-сегменты.
  - Возобновление сессий.

**Пример**:
- Браузер подключается к sazykin.ru.
- Выбирает DHE_AES_256, проверяет сертификат, использует DHE для
  ключей.

**Таблица: Итог TLS 1.2**:
| Элемент             | Роль                                  | Пример                  |
|---------------------|---------------------------------------|-------------------------|
| Handshake           | Выбор шифров, аутентификация, ключи   | ClientHello, Finished   |
| Сертификат          | Подлинность сервера                   | Let's Encrypt           |
| Обмен ключами       | Ключи для шифрования и MAC            | DHE, RSA                |
| CloseNotify         | Разрыв соединения                     | Завершение HTTPS        |

Summarize: TLS 1.2 устанавливает безопасное соединение через Handshake,
оптимизируя сессии и сообщения.