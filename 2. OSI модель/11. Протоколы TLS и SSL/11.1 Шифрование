

--------------------------------------------------------------------
Конспект лекции: Шифрование в TLS и SSL 🌐🔒
--------------------------------------------------------------------
Лекция из курса "Компьютерные сети" (Андрей Сазыкин). Тема: шифрование
в TLS/SSL для приватности (защита от пассивных атак). Конспект
структурирован с таблицами, примерами и связью с курсом. 😊

Summarize: TLS/SSL используют гибридное шифрование (симметричное +
асимметричное) для приватности данных.

--------------------------------------------------------------------
1. Введение: зачем шифрование в TLS/SSL? 🛡️📡
--------------------------------------------------------------------
- **TLS/SSL**: семейство протоколов для безопасной передачи данных в
  небезопасных сетях.
- **Безопасность**: 3 функции — приватность, целостность, аутентификация.
- **Приватность**: защита от пассивных атак (злоумышленник читает данные).
- **Метод**: шифрование данных, чтобы их нельзя было прочитать.

**Пример**: Злоумышленник перехватывает HTTPS-трафик, но без ключа не
может прочитать номер карты.

Summarize: Шифрование в TLS/SSL защищает данные от перехвата,
обеспечивая приватность.

--------------------------------------------------------------------
2. Основы шифрования: шифр Цезаря 📜🔑
--------------------------------------------------------------------
- **Шифр Цезаря**: древний шифр сдвига, использовался Юлием Цезарем.
- **Как работает**:
  - Каждая буква сдвигается на фиксированное число позиций (ключ).
  - Например, сдвиг +3: А → Г, Б → Д, В → Е.
  - Расшифровка: сдвиг в обратную сторону (–3).
- **Ключ**: величина и направление сдвига (например, +3).
- **Слабость**: легко взломать перебором (33 буквы в русском алфавите).

**Таблица: Пример шифра Цезаря**:
| Исходная буква | Зашифрованная (+3) | Расшифрованная (–3) |
|----------------|--------------------|---------------------|
| А              | Г                  | А                   |
| Б              | Д                  | Б                   |
| В              | Е                  | В                   |

**Пример**: Сообщение "КОД" (+3) → "НРЖ", (–3) → "КОД".

Summarize: Шифр Цезаря — простой шифр сдвига, уязвимый, но иллюстрирует
понятие ключа.

--------------------------------------------------------------------
3. Типы шифрования в TLS/SSL 🔐🛠️
--------------------------------------------------------------------
TLS/SSL используют два типа шифрования:
1. **Симметричное шифрование**:
   - Один ключ для шифрования и расшифровки (разделяемый ключ).
   - Быстрое, но ключ должен быть секретным.
   - Алгоритмы: AES, DES, 3DES, RC4, RC5, RC6.
2. **Асимметричное шифрование**:
   - Два ключа: открытый (для шифрования) и закрытый (для расшифровки).
   - Медленное, но открытый ключ можно передавать по открытым каналам.
   - Алгоритмы: RSA, DSA, DSS, Диффи-Хеллмана.

**Таблица: Сравнение типов шифрования**:
| Тип            | Ключи          | Скорость | Секретность ключа | Алгоритмы         |
|----------------|----------------|----------|-------------------|-------------------|
| Симметричное   | Один (общий)   | Быстрая  | Секретный         | AES, DES, RC4     |
| Асимметричное  | Открытый + закрытый | Медленная | Только закрытый | RSA, Диффи-Хеллмана |

**Пример**: AES шифрует данные HTTPS, RSA передаёт ключ AES.

Summarize: Симметричное — быстрое, но секретное; асимметричное —
безопасное, но медленное.

--------------------------------------------------------------------
4. Гибридное шифрование в TLS/SSL 🔄🔒
--------------------------------------------------------------------
- **Проблема**:
  - Симметричное: как передать секретный ключ по открытым каналам?
  - Асимметричное: слишком медленное для больших данных.
- **Решение**: гибридное шифрование.
  - Асимметричное шифрование передаёт ключ для симметричного.
  - Симметричное шифрование используется для данных.
- **Процесс**:
  1. Клиент и сервер обмениваются ключами через асимметричное
     шифрование.
  2. Используют полученный ключ для симметричного шифрования.

**Пример**: Клиент шифрует ключ AES с помощью открытого ключа RSA
сервера, сервер расшифровывает и использует AES для данных.

Summarize: Гибридное шифрование сочетает скорость симметричного и
безопасность асимметричного.

--------------------------------------------------------------------
5. Алгоритмы обмена ключами: RSA 📡🔑
--------------------------------------------------------------------
- **RSA**: асимметричный алгоритм (Ривест, Шамир, Адлеман).
- **Как работает**:
  1. Сервер имеет открытый и закрытый ключи.
  2. Клиент отправляет "Hello" для установки соединения.
  3. Сервер передаёт открытый ключ клиенту.
  4. Клиент генерирует ключ для симметричного шифрования, шифрует его
     открытым ключом и отправляет серверу.
  5. Сервер расшифровывает ключ закрытым ключом.
  6. Клиент и сервер используют симметричный ключ.
- **Недостатки**:
  - Нет **совершенной прямой секретности** (Perfect Forward Secrecy):
    если закрытый ключ сервера украден, прошлые данные можно расшифровать.
  - Уязвимости: атака миллиона сообщений (1998), атака ROBOT (2017).
- **Статус**: запрещён в TLS 1.3 из-за уязвимостей.

**Таблица: Процесс RSA**:
| Шаг | Действие                              | Кто выполняет |
|-----|---------------------------------------|---------------|
| 1   | Отправка "Hello"                      | Клиент        |
| 2   | Передача открытого ключа              | Сервер        |
| 3   | Генерация и шифрование сим. ключа     | Клиент        |
| 4   | Расшифровка сим. ключа закрытым ключом| Сервер        |
| 5   | Использование сим. ключа              | Оба           |

**Пример**: Клиент шифрует ключ AES открытым ключом RSA, сервер
расшифровывает закрытым.

Summarize: RSA передаёт симметричный ключ, но уязвим и не обеспечивает
совершенную секретность.

--------------------------------------------------------------------
6. Алгоритмы обмена ключами: Диффи-Хеллмана 🔄🔐
--------------------------------------------------------------------
- **Диффи-Хеллмана**: асимметричный алгоритм для обмена ключами.
- **Как работает**:
  1. Клиент и сервер договариваются о числах \( p \) (простое) и \( g \)
     (малое, открытое).
  2. Клиент и сервер генерируют секретные числа (не передаются).
  3. Клиент: \( g^{секрет_клиента} \mod p \), передаёт результат серверу.
  4. Сервер: \( g^{секрет_сервера} \mod p \), передаёт результат клиенту.
  5. Оба вычисляют общий ключ: \( (результат_другого)^{свой_секрет} \mod p \).
- **Пример** (\( p = 29, g = 3 \)):
  - Клиент: секрет = 5, \( 3^5 \mod 29 = 11 \), отправляет 11.
  - Сервер: секрет = 8, \( 3^8 \mod 29 = 7 \), отправляет 7.
  - Клиент: \( 7^5 \mod 29 = 16 \).
  - Сервер: \( 11^8 \mod 29 = 16 \).
  - Ключ: 16.
- **Преимущества**:
  - **Совершенная прямая секретность**: прошлые данные защищены, даже
    если сервер взломан.
  - Меньше уязвимостей, чем RSA.
- **Требования**:
  - \( p \): большое простое число (≥1024 бит).
  - \( g \): первообразный корень по модулю \( p \).
- **Вариант**: эллиптические кривые (ECDH) для большей эффективности.

**Таблица: Процесс Диффи-Хеллмана**:
| Шаг | Действие                              | Кто выполняет |
|-----|---------------------------------------|---------------|
| 1   | Согласование \( p, g \)               | Оба           |
| 2   | Генерация секретного числа            | Оба           |
| 3   | Вычисление \( g^{секрет} \mod p \)    | Оба           |
| 4   | Обмен результатами                    | Оба           |
| 5   | Вычисление общего ключа               | Оба           |

**Пример**: Клиент и сервер получают ключ 16 без передачи секретов.

Summarize: Диффи-Хеллмана обеспечивает безопасный обмен ключами с
совершенной секретностью.

--------------------------------------------------------------------
7. Набор шифров (Cipher Suite) 📋🔧
--------------------------------------------------------------------
- **Cipher Suite**: набор алгоритмов, используемых в TLS-соединении.
- **Состав**:
  - Алгоритм обмена ключами (RSA, Диффи-Хеллмана).
  - Алгоритм симметричного шифрования (AES, DES).
  - Алгоритмы для целостности и аутентификации (рассмотрят позже).
- **Процесс**:
  1. Клиент и сервер договариваются о Cipher Suite.
  2. Выполняют обмен ключами (асимметричное шифрование).
- **Пример**: `TLS_DHE_RSA_WITH_AES_256_CBC_SHA256`:
  - DHE: Диффи-Хеллмана с эллиптическими кривыми.
  - RSA: для аутентификации.
  - AES_256_CBC: симметричное шифрование.
  - SHA256: хэш-функция.

**Таблица: Пример Cipher Suite**:
| Компонент           | Алгоритм              | Роль                     |
|---------------------|-----------------------|--------------------------|
| Обмен ключами       | Диффи-Хеллмана (DHE)  | Передача сим. ключа      |
| Аутентификация      | RSA                   | Проверка сервера         |
| Шифрование          | AES_256_CBC           | Защита данных            |
| Целостность         | SHA256                | Проверка изменений       |

**Пример**: TLS 1.3 использует Cipher Suite с Диффи-Хеллмана и AES.

Summarize: Cipher Suite определяет алгоритмы для шифрования, обмена
ключами и защиты.

--------------------------------------------------------------------
8. Совершенная прямая секретность (Perfect Forward Secrecy) 🔒⏳
--------------------------------------------------------------------
- **Определение**: невозможность расшифровать прошлые данные, даже если
  закрытый ключ сервера украден.
- **RSA**: не поддерживает (уязвим, если ключ украден).
- **Диффи-Хеллмана**: поддерживает (секреты не хранятся).
- **TLS 1.3**: требует Диффи-Хеллмана для обеспечения секретности.

**Таблица: Совершенная прямая секретность**:
| Алгоритм       | Поддержка PFS | Использование в TLS |
|----------------|---------------|---------------------|
| RSA            | Нет           | Запрещён в TLS 1.3  |
| Диффи-Хеллмана | Да            | Обязателен в TLS 1.3|

**Пример**: В TLS 1.3 Диффи-Хеллмана защищает старые сессии HTTPS.

Summarize: Диффи-Хеллмана в TLS 1.3 обеспечивает PFS, RSA — нет.

--------------------------------------------------------------------
9. Связь с курсом "Компьютерные сети" 📚🔗
--------------------------------------------------------------------
- **TCP/IP**: TLS использует TCP для надёжной доставки (лекция про
  TCP/IP).
- **Безопасность**: Шифрование защищает от пассивных атак (лекция про
  безопасность).
- **HTTP/HTTPS**: TLS шифрует данные HTTPS (лекция про прикладные
  протоколы).
- **OSPF**: OSPF не шифрует данные, TLS добавляет безопасность (лекция
  про OSPF).
- **Криптография**: Курс объясняет основы шифрования (лекция про
  криптографию).

**Пример**: В сети университета OSPF маршрутизирует, TLS шифрует HTTPS.

Summarize: TLS связан с TCP/IP, HTTPS, безопасностью, дополняет OSPF.

--------------------------------------------------------------------
10. Итог: шифрование в TLS/SSL 🎯🔥
--------------------------------------------------------------------
- **Шифрование**: обеспечивает приватность (защита от перехвата).
- **Типы**:
  - Симметричное (AES, DES): быстрое, секретный ключ.
  - Асимметричное (RSA, Диффи-Хеллмана): безопасное, медленное.
- **Гибридное**: асимметричное для обмена ключами, симметричное для
  данных.
- **Алгоритмы**:
  - RSA: уязвим, без PFS, запрещён в TLS 1.3.
  - Диффи-Хеллмана: безопасен, с PFS, обязателен в TLS 1.3.
- **Cipher Suite**: набор алгоритмов для соединения.
- **PFS**: защита прошлых данных (Диффи-Хеллмана).

**Пример**: В HTTPS TLS 1.3 использует Диффи-Хеллмана для обмена
ключом AES, защищая данные.

Summarize: TLS/SSL используют гибридное шифрование с Диффи-Хеллмана для
приватности и PFS.