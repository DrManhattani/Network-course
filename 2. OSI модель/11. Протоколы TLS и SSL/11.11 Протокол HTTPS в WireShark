

--------------------------------------------------------------------
Анализ протокола HTTPS в Wireshark 🌐🔍
--------------------------------------------------------------------
Практика показывает, как анализировать HTTPS в Wireshark: настройка
Chrome, захват пакетов, расшифровка HTTP с ключами TLS. 😊

Summarize: HTTPS (HTTP поверх TLS 1.3) шифрует данные, Wireshark
расшифровывает HTTP-запросы с ключами.

--------------------------------------------------------------------
1. Введение: зачем анализировать HTTPS? 🛡️📡
--------------------------------------------------------------------
- **HTTPS**: HTTP поверх TLS, защищает веб-трафик (шифрование, целостность).
- **Признак**: замок в браузере, `https://` в URL.
- **Цель**: изучить HTTPS в Wireshark, включая HTTP-запросы и ответы.
- **Проблема**: данные зашифрованы TLS, нужны ключи для расшифровки.
- **Инструменты**:
  - Chrome: браузер с сохранением ключей TLS.
  - Wireshark: захват и анализ пакетов.
- **Процесс**:
  1. Настроить Chrome для сохранения ключей.
  2. Установить фильтры в Wireshark.
  3. Открыть сайт (sazykin.ru) и захватить трафик.
  4. Расшифровать HTTP с ключами.

**Пример**: Анализ `GET /` для sazykin.ru в Wireshark.

**Таблица: Цели анализа**:
| Аспект            | Описание                          | Инструмент                |
|-------------------|-----------------------------------|---------------------------|
| Протокол          | HTTPS (TLS 1.3)                   | Wireshark                 |
| Браузер           | Сохранение ключей                 | Chrome                    |
| Сайт              | Тестовый HTTPS                    | sazykin.ru                |
| Ключи             | Расшифровка                       | Файл ключей               |

Summarize: Wireshark анализирует HTTPS, расшифровывая HTTP с ключами.

--------------------------------------------------------------------
2. Настройка Chrome для сохранения ключей 🖥️🔑
--------------------------------------------------------------------
- **Цель**: сохранить ключи TLS для расшифровки.
- **Способ**: ярлык Chrome с параметром.
  - Параметр: `--ssl-key-log-file=<путь_к_файлу>`.
  - Пример: `chrome --ssl-key-log-file=C:\keys.log`.
  - Дополнительно: `--ssl-version-max=tls1.3` (TLS 1.3).
- **Процесс**:
  1. Создать ярлык Chrome с параметром.
  2. Запустить браузер через ярлык.
  3. Открыть сайт (sazykin.ru).
  4. Проверить файл ключей (`C:\keys.log`).
- **Формат файла ключей**:
  - `CLIENT_RANDOM <ClientRandom> <Premaster Secret>`.
- **Безопасность**:
  - Удалить ярлык после экспериментов.
  - Удалить файл ключей.

**Пример**: Ярлык Chrome создаёт `C:\keys.log` для sazykin.ru.

**Таблица: Параметры Chrome**:
| Параметр                  | Значение                  | Роль                      |
|---------------------------|---------------------------|---------------------------|
| `--ssl-version-max`       | `tls1.3`                  | Принудительный TLS 1.3    |
| `--ssl-key-log-file`      | `C:\keys.log`             | Сохранение ключей         |

Summarize: Ярлык Chrome сохраняет ключи TLS 1.3 в файл.

--------------------------------------------------------------------
3. Настройка Wireshark для анализа HTTPS 📡🔧
--------------------------------------------------------------------
- **Фильтры**:
  - Протокол: `tls`.
  - IP-адрес: IP сайта (sazykin.ru).
  - Пример: `tls && ip.addr == <IP_sazykin.ru>`.
- **IP-адрес**:
  - Получить: `nslookup sazykin.ru` в командной строке.
- **Ключи**:
  - Путь: `Редактирование → Параметры → Протоколы → TLS`.
  - Поле: `Pre-Master-Secret log filename`.
  - Указать: `C:\keys.log`.
- **Процесс**:
  1. Установить фильтры по TLS и IP.
  2. Подключить файл ключей.
  3. Запустить захват пакетов.
  4. Открыть сайт в Chrome.
- **Результат**:
  - Без ключей: зашифрованные данные (Application Data).
  - С ключами: расшифровка HTTP-запросов и ответов.

**Пример**: Wireshark с фильтром `tls` и `C:\keys.log` для sazykin.ru.

**Таблица: Настройка Wireshark**:
| Параметр          | Значение                          | Роль                      |
|-------------------|-----------------------------------|---------------------------|
| Фильтр            | `tls && ip.addr == IP`            | Ограничение трафика       |
| Pre-Master-Secret | `C:\keys.log`                     | Расшифровка TLS           |

Summarize: Wireshark с фильтрами и ключами расшифровывает HTTPS.

--------------------------------------------------------------------
4. Установка соединения: TCP и TLS 1.3 📜🔄
--------------------------------------------------------------------
- **TCP-соединение**:
  - Клиент устанавливает два соединения (для ускорения).
  - Пакеты:
    1. Клиент: SYN.
    2. Сервер: SYN+ACK.
    3. Клиент: ACK.
  - Порты:
    - Сервер: 443 (HTTPS).
    - Клиент: случайный (например, 56995).
- **TLS 1.3 Handshake** (неудачный выбор шифров):
  1. **ClientHello** (первое):
     - ClientRandom: для ключей.
     - Шифры: AEAD (AES-GCM, ChaCha20-Poly1305).
     - Key Share: параметры ECDHE.
     - Record Layer: Handshake Protocol.
  2. **HelloRetryRequest + ChangeCipherSpec**:
     - Сервер: шифр не поддерживается, предлагает новый (AES_256_GCM_SHA384).
     - ChangeCipherSpec: начало шифрования.
  3. **ClientHello** (повторное) + **ChangeCipherSpec**:
     - Клиент: новый Key Share по запросу сервера.
     - ChangeCipherSpec: клиент готов шифровать.
  4. **ServerHello + Application Data**:
     - Сервер: подтверждает шифр, отправляет зашифрованные данные.
  5. **Certificate, Finished** (расшифровано с ключами):
     - Сертификаты: sazykin.ru, Let's Encrypt.
     - Finished: завершение Handshake.
  6. **Client Finished**:
     - Клиент подтверждает шифрование.

**Пример**: Два TCP-соединения и TLS Handshake для sazykin.ru.

**Таблица: Этапы соединения**:
| Этап              | Протокол | Сообщение                        | Роль                      |
|-------------------|----------|----------------------------------|---------------------------|
| TCP               | TCP      | SYN, SYN+ACK, ACK                | Установка соединения      |
| TLS Handshake     | TLS 1.3  | ClientHello, ServerHello, Finished | Установка TLS             |

Summarize: HTTPS начинается с TCP (порт 443) и TLS 1.3 Handshake.

--------------------------------------------------------------------
5. Передача данных: HTTP в TLS Application Data 📦🔓
--------------------------------------------------------------------
- **Без ключей**:
  - Wireshark: Application Data, данные зашифрованы.
- **С ключами**:
  - Расшифровка:
    - Запрос: `GET / HTTP/1.1`.
      - Заголовки:
        - `Host: sazykin.ru` (SNI для виртуального хостинга).
        - `Connection: keep-alive` (поддержание соединения).
      - Порт: 443 (сервер), 56995 (клиент).
    - Ответ: `HTTP/1.1 304 Not Modified` (кеш браузера).
      - Заголовки:
        - `Server: nginx` (веб-сервер).
        - `Connection: keep-alive`.
        - `Cache-Control`: управление кешем.
      - Порт: 443 (сервер), 56995 (клиент).
    - Полная загрузка (принудительное обновление, Ctrl+F5):
      - Ответ: `HTTP/1.1 200 OK`.
      - Данные: HTML (text/html, UTF-8).
      - Заголовки: `Last-Modified`, `Content-Type`.
      - Сегменты: 6 TLS-сегментов для страницы.
- **Структура**:
  - HTTP → TLS (Application Data) → TCP → IP.
  - Формат HTTP: текстовый, как в HTTP.

**Пример**: Wireshark показывает `GET /` и `304` для sazykin.ru.

**Таблица: HTTP в HTTPS**:
| Состояние         | Вид в Wireshark                   | Содержимое                |
|-------------------|-----------------------------------|---------------------------|
| С ключами         | `GET /`, `304` или `200`          | Расшифрованный HTTP       |
| Без ключей        | Application Data                  | Нечитаемые данные         |

Summarize: HTTP-запросы шифруются TLS, расшифровываются с ключами.

--------------------------------------------------------------------
6. Принудительное обновление страницы 🔄📄
--------------------------------------------------------------------
- **Цель**: загрузить страницу без кеша (Ctrl+F5).
- **Результат**:
  - Запрос: `GET / HTTP/1.1`.
  - Ответ: `HTTP/1.1 200 OK`.
    - Данные: HTML-страница (text/html, UTF-8).
    - Заголовки: `Last-Modified`, `Content-Type`, `Connection: keep-alive`.
    - Сегменты: 6 TLS-сегментов.
- **Отличие от кеша**:
  - `304 Not Modified`: кеш, без данных.
  - `200 OK`: полная загрузка страницы.

**Пример**: Ctrl+F5 для sazykin.ru даёт `200 OK` и HTML.

**Таблица: Ответы сервера**:
| Статус            | Описание                          | Данные                    |
|-------------------|-----------------------------------|---------------------------|
| `304 Not Modified`| Кеш, страница не изменилась       | Нет данных                |
| `200 OK`          | Полная загрузка                   | HTML, 6 сегментов         |

Summarize: Ctrl+F5 загружает полную страницу с `200 OK`.

--------------------------------------------------------------------
7. Безопасность HTTPS: защита и ключи 🔒⚠️
--------------------------------------------------------------------
- **Защита HTTPS**:
  - Шифрование: злоумышленник не читает данные (AES_256_GCM_SHA384).
  - Целостность: защита от изменений (SHA384 в AEAD).
  - Аутентификация: сертификаты (sazykin.ru, Let's Encrypt).
- **Риски**:
  - Утечка файла ключей → расшифровка трафика.
- **Рекомендации**:
  - Удалить ярлык Chrome после экспериментов.
  - Удалить `C:\keys.log`.
  - Ограничить доступ к файлу ключей.

**Пример**: Удаление `C:\keys.log` защищает ключи sazykin.ru.

**Таблица: Меры безопасности**:
| Действие                  | Цель                              | Риск без действия         |
|---------------------------|-----------------------------------|---------------------------|
| Удалить ярлык             | Прекратить запись ключей          | Постоянная запись         |
| Удалить файл ключей       | Защита ключей                     | Утечка ключей             |

Summarize: HTTPS защищает данные, ключи требуют осторожности.

--------------------------------------------------------------------
8. Связь с курсом "Компьютерные сети" 📚🔗
--------------------------------------------------------------------
- **TCP/IP**: HTTPS работает поверх TCP (порт 443) (лекция про TCP/IP).
- **HTTP**: HTTPS защищает HTTP-запросы (лекция про прикладные протоколы).
- **TLS**: HTTPS использует TLS 1.3 (лекции про TLS).
- **DNS**: `nslookup` для IP сайта (лекция про DNS).
- **Шифрование**: AES-GCM, SHA384 (лекция 2).
- **PKI**: Сертификаты X.509, Let's Encrypt (лекция 4).
- **OSPF**: OSPF маршрутизирует без шифрования, HTTPS защищает (лекция про OSPF).

**Пример**:
- OSPF маршрутизирует пакеты.
- HTTPS защищает `GET /` для sazykin.ru.

**Таблица: Связь с курсом**:
| Тема              | Связь с HTTPS                     | Пример                    |
|-------------------|-----------------------------------|---------------------------|
| TCP/IP            | HTTPS поверх TCP (порт 443)       | TCP-соединение            |
| HTTP              | HTTPS защищает HTTP               | `GET / sazykin.ru`        |
| TLS               | Шифрование, аутентификация        | TLS 1.3 Handshake         |
| DNS               | `nslookup` для IP                 | IP sazykin.ru             |
| PKI               | Сертификаты X.509                 | Let's Encrypt             |
| OSPF              | OSPF без шифрования, HTTPS защищает | Маршруты + HTTPS         |

Summarize: HTTPS связан с TCP/IP, HTTP, TLS, DNS, дополняет OSPF.

--------------------------------------------------------------------
9. Итог: анализ HTTPS в Wireshark 🎯🔥
--------------------------------------------------------------------
- **Процесс**:
  - Настроить Chrome: `--ssl-key-log-file`, TLS 1.3.
  - Настроить Wireshark: фильтры `tls`, файл ключей.
  - Захватить трафик sazykin.ru.
- **HTTPS**:
  - TCP (порт 443) → TLS 1.3 Handshake → HTTP в Application Data.
  - Запрос: `GET /`, ответ: `304` или `200 OK`.
  - Формат: текстовый HTTP, шифрованный TLS.
- **С ключами**:
  - Видны: `GET /`, `304`, `200 OK`, HTML.
  - Сертификаты: sazykin.ru, Let's Encrypt.
- **Без ключей**:
  - Только Application Data.
- **Безопасность**:
  - Удалить ярлык и файл ключей.

**Пример**:
- Wireshark расшифровывает `GET /` и `200 OK` для sazykin.ru.

**Таблица: Ключевые элементы HTTPS**:
| Элемент           | Роль                              | Пример                    |
|-------------------|-----------------------------------|---------------------------|
| TCP               | Соединение (порт 443)             | SYN, ACK                  |
| TLS 1.3           | Шифрование, Handshake             | ClientHello, Finished     |
| HTTP              | Запросы и ответы                  | `GET /`, `200 OK`         |
| Ключи             | Расшифровка                       | `C:\keys.log`             |
| Сертификаты       | Аутентификация                    | Let's Encrypt             |

Summarize: HTTPS — HTTP в TLS, Wireshark расшифровывает с ключами.