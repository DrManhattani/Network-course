

--------------------------------------------------------------------
Расшифровка TLS в Wireshark 🌐🔍
--------------------------------------------------------------------
Лекция показывает, как расшифровать TLS 1.2 в Wireshark с помощью ключей
из браузера: настройка Chrome, Wireshark и анализ трафика. 😊

Summarize: Wireshark расшифровывает TLS 1.2 с ключами из файла,
показывая HTTP-запросы и разрыв соединения.

--------------------------------------------------------------------
1. Зачем расшифровывать TLS в Wireshark? 🛡️🔐
--------------------------------------------------------------------
- **Цель**: увидеть содержимое TLS-трафика (например, HTTP-запросы).
- **Проблема**: TLS шифрует данные (AES, SHA256), ключи уникальны для
  каждой сессии.
- **Решение**: использовать ключи сессии (Premaster Secret).
- **Диффи-Хеллман (ECDHE)**:
  - Обеспечивает прямую секретность (лекция 2).
  - Ключи генерируются для каждого соединения.
- **Ключи**:
  - ClientRandom: случайное число из ClientHello.
  - Premaster Secret: основа для ключей шифрования.
- **Источник ключей**: браузеры (Chrome, Firefox) могут сохранять ключи.

**Пример**: Расшифровка HTTPS-запроса к sazykin.ru в Wireshark.

**Таблица: Роль ключей в TLS**:
| Ключ              | Описание                          | Источник                  |
|-------------------|-----------------------------------|---------------------------|
| ClientRandom      | Случайное число клиента           | ClientHello               |
| Premaster Secret  | Основа для ключей шифрования      | Обмен ключами (ECDHE)     |
| Ключи сессии      | Для AES и SHA256                  | Генерируются из Premaster |

Summarize: Wireshark расшифровывает TLS с ключами из браузера.

--------------------------------------------------------------------
2. Настройка браузера для сохранения ключей 🖥️🔑
--------------------------------------------------------------------
- **Браузер**: Chrome (аналогично для Firefox).
- **Способы сохранения ключей**:
  1. **Переменная окружения**:
     - `SSLKEYLOGFILE=<путь_к_файлу>`.
     - Небезопасно: ключи записываются постоянно.
  2. **Скрипт**:
     - Устанавливает `SSLKEYLOGFILE` и запускает браузер.
  3. **Ярлык Chrome** (рекомендуемый):
     - Параметры: `--ssl-key-log-file=<путь_к_файлу>`.
     - Пример: `chrome --ssl-key-log-file=C:\keys.log`.
     - Дополнительно: `--ssl-version-max=tls1.2` (для TLS 1.2).
- **Процесс**:
  1. Остановить работающий браузер.
  2. Создать ярлык с параметрами.
  3. Запустить Chrome через ярлык.
  4. Открыть сайт (например, wireshark.org).
  5. Проверить файл ключей.
- **Формат файла ключей**:
  - `CLIENT_RANDOM <ClientRandom> <Premaster Secret>`.
  - Пример: `CLIENT_RANDOM 1234... abcd...`.
- **Примечания**:
  - Иногда требуется перезагрузка ПК.
  - Удалить ярлык после использования (безопасность).

**Пример**: Ярлык Chrome создаёт `C:\keys.log` для sazykin.ru.

**Таблица: Способы сохранения ключей**:
| Способ            | Описание                          | Плюсы                     | Минусы                    |
|-------------------|-----------------------------------|---------------------------|---------------------------|
| Переменная        | `SSLKEYLOGFILE`                   | Простота                  | Небезопасно               |
| Скрипт            | Автоматизация                     | Контроль                  | Сложнее                   |
| Ярлык             | `--ssl-key-log-file`              | Безопаснее                | Ручная настройка          |

Summarize: Ярлык Chrome с `--ssl-key-log-file` сохраняет ключи безопасно.

--------------------------------------------------------------------
3. Настройка Wireshark для расшифровки 📡🔧
--------------------------------------------------------------------
- **Шаг 1**: Указать файл ключей в Wireshark.
  - Путь: `Редактирование → Параметры → Протоколы → TLS`.
  - Поле: `Pre-Master-Secret log filename`.
  - Указать: путь к файлу ключей (например, `C:\keys.log`).
- **Шаг 2**: Настроить фильтры.
  - Протокол: `tls`.
  - IP-адрес: IP сайта (например, sazykin.ru).
  - Пример: `tls && ip.addr == <IP_sazykin.ru>`.
- **Шаг 3**: Захватить пакеты.
  - Открыть сайт в Chrome (через ярлык).
  - Запустить захват в Wireshark.
- **Результат**:
  - Wireshark расшифровывает TLS-трафик (HTTP-запросы, ответы).

**Пример**: Wireshark использует `C:\keys.log` для sazykin.ru.

**Таблица: Настройка Wireshark**:
| Параметр          | Значение                          | Роль                      |
|-------------------|-----------------------------------|---------------------------|
| Фильтр            | `tls && ip.addr == IP`            | Ограничение трафика       |
| Pre-Master-Secret | `C:\keys.log`                     | Файл ключей               |
| Протокол          | TLS                               | Расшифровка TLS           |

Summarize: Wireshark расшифровывает TLS с файлом ключей и фильтрами.

--------------------------------------------------------------------
4. Анализ TLS 1.2 в Wireshark: установка соединения 📜🔄
--------------------------------------------------------------------
- **Протокол**: TLS 1.2 (TLS 1.3 в следующем видео).
- **Наблюдения**:
  - Два соединения для ускорения.
  - Handshake: ClientHello, ServerHello, Certificate, KeyExchange.
- **Сообщения**:
  1. **ClientHello**:
     - ClientRandom, шифры, SNI (sazykin.ru).
  2. **ServerHello**:
     - ServerRandom, шифр (например, ECDHE_RSA_AES_256_CBC_SHA256).
  3. **Certificate**:
     - Сертификат sazykin.ru (Let's Encrypt).
  4. **ServerKeyExchange**:
     - Параметры ECDHE (для ключей).
  5. **ClientKeyExchange**:
     - Параметры клиента (yc для ECDHE).
  6. **ChangeCipherSpec**:
     - Готовность к шифрованию.
  7. **Finished**:
     - Зашифрованное подтверждение (AES_256, SHA256).
- **Структура пакета**:
  - TLS → TCP → IP.
  - Record Protocol: тип (Handshake, Application Data).

**Пример**: Wireshark показывает Handshake для sazykin.ru.

**Таблица: Сообщения Handshake**:
| Сообщение          | Содержимое                        | Роль                      |
|--------------------|-----------------------------------|---------------------------|
| ClientHello        | Шифры, ClientRandom, SNI          | Предложение параметров    |
| ServerHello        | Шифр, ServerRandom                | Выбор параметров          |
| Certificate        | Сертификат sazykin.ru             | Аутентификация            |
| ServerKeyExchange  | Параметры ECDHE                   | Обмен ключами             |
| ChangeCipherSpec   | Готовность к шифрованию           | Переход к шифрованию      |
| Finished           | Зашифрованное подтверждение       | Завершение Handshake      |

Summarize: Wireshark показывает Handshake TLS 1.2: шифры, ключи, сертификаты.

--------------------------------------------------------------------
5. Расшифровка данных: Application Data 📦🔓
--------------------------------------------------------------------
- **Роль**: Просмотр HTTP-трафика внутри TLS.
- **Record Protocol**:
  - Content Type: Application Data.
  - Данные: зашифрованный HTTP (AES_256, SHA256).
- **С расшифровкой**:
  - Wireshark показывает HTTP-запросы и ответы.
  - Пример:
    - Запрос: `GET / HTTP/1.1` (sazykin.ru).
    - Ответ: `HTTP/1.1 304 Not Modified`.
- **Без ключей**:
  - Данные: нечитаемые (Application Data).
- **Примечание**:
  - Ответ `304 Not Modified`: страница не изменилась (кеш браузера).

**Пример**: Wireshark расшифровывает `GET /` и `304` для sazykin.ru.

**Таблица: Application Data**:
| Состояние         | Вид в Wireshark                   | Содержимое                |
|-------------------|-----------------------------------|---------------------------|
| С ключами         | HTTP (GET, 304)                   | Расшифрованный HTTP       |
| Без ключей        | Application Data                  | Нечитаемые данные         |

Summarize: Wireshark расшифровывает HTTP внутри TLS с ключами.

--------------------------------------------------------------------
6. Разрыв соединения: CloseNotify 🚪🔐
--------------------------------------------------------------------
- **Протокол**: Alert Protocol (лекция 5).
- **Сообщение**: CloseNotify (Encrypted Alert).
- **С расшифровкой**:
  - Wireshark показывает CloseNotify (разрыв).
  - Два соединения → два CloseNotify.
- **Без ключей**:
  - Wireshark видит только Encrypted Alert.
- **Роль**: Завершение TLS-соединения.

**Пример**: Wireshark показывает два CloseNotify для sazykin.ru.

**Таблица: Разрыв соединения**:
| Состояние         | Сообщение                         | Вид в Wireshark           |
|-------------------|-----------------------------------|---------------------------|
| С ключами         | CloseNotify                       | Разрыв соединения         |
| Без ключей        | Encrypted Alert                   | Нечитаемое уведомление    |

Summarize: CloseNotify завершает TLS-соединение, видно с ключами.

--------------------------------------------------------------------
7. Безопасность: защита ключей 🔒⚠️
--------------------------------------------------------------------
- **Риски**:
  - Доступ к файлу ключей → расшифровка трафика.
  - Постоянная запись ключей (через `SSLKEYLOGFILE`).
- **Рекомендации**:
  - Удалить ярлык Chrome после использования.
  - Удалить переменную `SSLKEYLOGFILE` (если использовалась).
  - Ограничить доступ к файлу ключей.
- **Тест**:
  - Убрать путь к файлу в Wireshark → данные не расшифровываются.
  - Вернуть путь → расшифровка работает.

**Пример**: Удаление `C:\keys.log` защищает ключи sazykin.ru.

**Таблица: Меры безопасности**:
| Действие                  | Цель                              | Риск без действия         |
|---------------------------|-----------------------------------|---------------------------|
| Удалить ярлык             | Прекратить запись ключей          | Постоянная запись         |
| Удалить `SSLKEYLOGFILE`   | Прекратить запись ключей          | Утечка ключей             |
| Ограничить доступ         | Защита файла ключей               | Расшифровка злоумышленником |

Summarize: Удаление ярлыка и переменной защищает ключи от утечки.

--------------------------------------------------------------------
8. Связь с курсом "Компьютерные сети" 📚🔗
--------------------------------------------------------------------
- **TCP/IP**: TLS работает поверх TCP (лекция про TCP/IP).
- **HTTP**: TLS защищает HTTP-запросы (лекция про прикладные протоколы).
- **Шифрование**: ECDHE, AES, SHA256 (лекция 2).
- **Целостность**: MAC с SHA256 (лекция 3).
- **PKI**: Сертификаты X.509 (лекция 4).
- **OSPF**: OSPF маршрутизирует без шифрования, TLS защищает (лекция про OSPF).

**Пример**:
- OSPF маршрутизирует пакеты.
- TLS защищает HTTP-запрос к sazykin.ru.

**Таблица: Связь с курсом**:
| Тема              | Связь с TLS                           | Пример                  |
|-------------------|---------------------------------------|-------------------------|
| TCP/IP            | TLS поверх TCP                        | HTTPS на порт 443       |
| HTTP              | TLS защищает HTTP                     | GET / sazykin.ru        |
| Шифрование        | ECDHE, AES                            | Шифрование HTTPS        |
| PKI               | Сертификаты X.509                     | Let's Encrypt           |
| OSPF              | OSPF без шифрования, TLS защищает     | Маршруты + HTTPS        |

Summarize: TLS связан с TCP/IP, HTTP, криптографией, дополняет OSPF.

--------------------------------------------------------------------
9. Итог: расшифровка TLS в Wireshark 🎯🔥
--------------------------------------------------------------------
- **Процесс**:
  - Настроить Chrome: ярлык с `--ssl-key-log-file`.
  - Настроить Wireshark: фильтры (`tls`, IP), путь к файлу ключей.
  - Захватить и расшифровать трафик.
- **TLS 1.2**:
  - Handshake: ClientHello, ServerHello, Certificate, Finished.
  - Application Data: HTTP (GET, 304).
  - Разрыв: CloseNotify.
- **Результаты**:
  - С ключами: видны HTTP-запросы, CloseNotify.
  - Без ключей: только Application Data, Encrypted Alert.
- **Безопасность**:
  - Удалить ярлык и переменную `SSLKEYLOGFILE`.
  - Ограничить доступ к файлу ключей.
- **Пример**:
  - Wireshark расшифровывает `GET /` и `304` для sazykin.ru.

- **Таблица**: Ключевые элементы для расшифровки:
  | Элемент | Роль | Пример |
  |------------|-------------------------------|
  | Ярлык Chrome     | Сохранение ключей в файл         | `--ssl-key-log-file`         |
  | Фильтры Wireshark | `tls` && IP                     | sazykin.ru                   |
  | Application Data | HTTP-запросы                   | `GET /`, 304                 |
  | CloseNotify       | Разрыв соединения               | Два уведомления              |
  | Файл ключей       | Расшифровка                     | `CLIENT_RANDOM, Premaster`   |

Summarize: Wireshark расшифровывает TLS 1.2 с ключами, показывая HTTP и CloseNotify.