

--------------------------------------------------------------------
Конспект лекции: Целостность данных в TLS и SSL 🌐🔒
--------------------------------------------------------------------
Лекция из курса "Компьютерные сети" (Андрей Сазыкин). Тема: обеспечение
целостности данных в TLS/SSL (защита от активных атак). Конспект
структурирован с таблицами, примерами и связью с курсом. 😊

Summarize: TLS/SSL используют криптографические хэш-функции и MAC для
защиты данных от изменений.

--------------------------------------------------------------------
1. Введение: целостность в TLS/SSL 🛡️📡
--------------------------------------------------------------------
- **TLS/SSL**: протоколы для безопасной передачи данных в небезопасных
  сетях.
- **Функции безопасности**: приватность, целостность, аутентификация.
- **Целостность**: защита от активных атак, когда злоумышленник изменяет
  данные так, чтобы клиент/сервер не заметили.
- **Проблема**: зашифрованные данные сложно, но можно изменить (например,
  обрезать сообщение).

**Пример**: Злоумышленник обрезает HTTPS-пакет, сервер принимает
неполное сообщение как правильное.

Summarize: Целостность защищает от преднамеренных и случайных изменений
данных.

--------------------------------------------------------------------
2. Целостность в сетевых технологиях 📜🔍
--------------------------------------------------------------------
- **Проблема изменений**: данные могут быть изменены злоумышленником или
  из-за ошибок в канале связи.
- **Примеры технологий**:
  - **Ethernet**: использует CRC32 (контрольная сумма) в концевике кадра.
  - **IP, Wi-Fi**: также имеют механизмы проверки целостности.
- **CRC32**:
  - Подходит для канального уровня (обнаружение случайных ошибок).
  - Недостаточен для транспортного уровня (уязвим к злонамеренным
    изменениям).

**Таблица: CRC32 в Ethernet**:
| Шаг          | Действие                              | Результат            |
|--------------|---------------------------------------|----------------------|
| Отправка     | Расчёт CRC32 для кадра                | Добавлен в конецвик  |
| Получение    | Перерасчёт CRC32                      | Сравнение с конецвиком |
| Совпадение   | Данные приняты                        | Кадр корректен       |
| Несовпадение | Кадр отбрасывается                    | Ошибка передачи      |

**Пример**: В Ethernet CRC32 обнаруживает битовые ошибки, но не защищает
от атак.

Summarize: CRC32 подходит для случайных ошибок, но не для атак на
транспортном уровне.

--------------------------------------------------------------------
3. Криптографические хэш-функции в TLS/SSL 🔐🛠️
--------------------------------------------------------------------
- **Хэш-функция**: преобразует данные любой длины в строку фиксированной
  длины (хэш).
- **Свойства**:
  - Быстрое вычисление хэша.
  - Невозможно восстановить данные по хэшу.
  - Минимальные коллизии (разные данные → разные хэши).
  - Сильное изменение хэша при малом изменении данных.
- **Криптографические хэш-функции**:
  - Используют алгоритмы, схожие с шифрованием.
  - Хэш выглядит случайным, устойчив к атакам.
- **Популярные алгоритмы**:
  - **MD5**: устарел, уязвим, не используется в TLS.
  - **SHA**: Secure Hash Algorithm (SHA-1, SHA-224, SHA-256, SHA-384,
    SHA-512).
  - SHA-256: хэш 256 бит, популярен в TLS.

**Таблица: Хэш-функции в TLS**:
| Алгоритм | Длина хэша | Статус         | Применение в TLS |
|----------|------------|----------------|------------------|
| MD5      | 128 бит    | Устарел, уязвим| Не используется  |
| SHA-1    | 160 бит    | Устарел        | Ограниченно      |
| SHA-256  | 256 бит    | Актуален       | Широко используется |
| SHA-512  | 512 бит    | Актуален       | Используется     |

**Пример**: SHA-256 генерирует хэш для HTTPS-пакета, изменение бита
меняет хэш.

Summarize: TLS использует SHA для хэшей, MD5 и SHA-1 устарели из-за
уязвимостей.

--------------------------------------------------------------------
4. Как работает целостность в TLS/SSL? 🔄🔒
--------------------------------------------------------------------
- **Процесс**:
  1. Клиент вычисляет хэш данных с помощью криптографической функции
     (например, SHA-256).
  2. Хэш отправляется вместе с данными в пакете.
  3. Сервер получает пакет, вычисляет хэш данных и сравнивает с
     полученным хэшем.
  - Совпадение: данные не изменены.
  - Несовпадение: данные изменены (ошибка или атака).
- **Проблема**: злоумышленник может перехватить пакет, изменить данные и
  пересчитать хэш.
- **Решение**: использование **Message Authentication Code (MAC)**.

**Пример**: Клиент отправляет данные + хэш SHA-256, сервер проверяет
совпадение хэшей.

Summarize: Хэш-функции обнаруживают изменения, но уязвимы к атакам без
ключа.

--------------------------------------------------------------------
5. Message Authentication Code (MAC) 📋🔑
--------------------------------------------------------------------
- **MAC**: код аутентификации сообщения (не путать с MAC-адресом в
  Ethernet).
- **Как работает**:
  - Вычисляется на основе данных и разделяемого ключа (есть только у
    клиента и сервера).
  - Алгоритм: хэш (данные + ключ).
  - Отправляется с данными в пакете.
- **Процесс**:
  1. Клиент: вычисляет MAC (данные + ключ).
  2. Сервер: получает данные, вычисляет MAC с тем же ключом, сравнивает.
  - Совпадение: данные не изменены.
  - Несовпадение: данные изменены (атака или ошибка).
- **Преимущество**: злоумышленник без ключа не может подделать MAC.
- **Название по-русски**: имитавставка.

**Таблица: Процесс MAC в TLS**:
| Шаг          | Действие                              | Кто выполняет |
|--------------|---------------------------------------|---------------|
| 1            | Вычисление MAC (данные + ключ)        | Клиент        |
| 2            | Отправка данных + MAC                 | Клиент        |
| 3            | Перерасчёт MAC (данные + ключ)        | Сервер        |
| 4            | Сравнение MAC                         | Сервер        |
| 5            | Принятие/отклонение данных            | Сервер        |

**Пример**: В HTTPS MAC (SHA-256 + ключ) защищает данные от подделки.

Summarize: MAC использует ключ для защиты хэша от подделки
злоумышленником.

--------------------------------------------------------------------
6. Набор шифров (Cipher Suite) и MAC 📋🔧
--------------------------------------------------------------------
- **Cipher Suite**: набор алгоритмов для TLS-соединения.
- **Компоненты**:
  - Алгоритм обмена ключами (RSA, Диффи-Хеллмана).
  - Алгоритм симметричного шифрования (AES).
  - Криптографическая хэш-функция для MAC (SHA-256).
- **Роль обмена ключами**:
  - Передача ключей для симметричного шифрования.
  - Передача ключей для генерации MAC.
- **Пример Cipher Suite**: `TLS_DHE_RSA_WITH_AES_256_CBC_SHA256`:
  - DHE: Диффи-Хеллмана (обмен ключами).
  - RSA: аутентификация.
  - AES_256_CBC: шифрование.
  - SHA256: хэш для MAC.

**Таблица: Компоненты Cipher Suite**:
| Компонент           | Алгоритм              | Роль                     |
|---------------------|-----------------------|--------------------------|
| Обмен ключами       | Диффи-Хеллмана        | Ключи для шифр./MAC      |
| Аутентификация      | RSA                   | Проверка сервера         |
| Шифрование          | AES_256_CBC           | Защита данных            |
| MAC                 | SHA256                | Целостность данных       |

**Пример**: TLS 1.3 использует SHA-256 для MAC в HTTPS.

Summarize: Cipher Suite включает хэш-функцию для MAC, обеспечивая
целостность.

--------------------------------------------------------------------
7. Ограничения и атаки 🔐⚠️
--------------------------------------------------------------------
- **Ограничение MAC**: подтверждает целостность и наличие ключа у
  отправителя, но не гарантирует, что отправитель — тот, за кого себя
  выдаёт.
- **Атака "человек посередине"**:
  - Злоумышленник перехватывает обмен ключами (например, Диффи-Хеллмана).
  - Подменяет ключи, устанавливая соединение с клиентом.
- **Решение**: цифровая подпись и инфраструктура открытых ключей (PKI),
  рассмотрят в следующей лекции.

**Пример**: Злоумышленник подменяет ключ Диффи-Хеллмана, клиент общается
с ним через TLS.

Summarize: MAC защищает от изменений, но не от атак на аутентификацию.

--------------------------------------------------------------------
8. Связь с курсом "Компьютерные сети" 📚🔗
--------------------------------------------------------------------
- **TCP/IP**: TLS работает на транспортном уровне, поверх TCP (лекция
  про TCP/IP).
- **Ethernet/Wi-Fi**: CRC32 для целостности на канальном уровне, TLS —
  на транспортном (лекция про канальный уровень).
- **Шифрование**: MAC использует ключи, как в симметричном шифровании
  (лекция про шифрование).
- **OSPF**: OSPF не защищает данные, TLS добавляет целостность (лекция
  про OSPF).
- **Криптография**: Хэш-функции — основа безопасности TLS (лекция про
  криптографию).

**Пример**: В сети университета TLS обеспечивает целостность HTTPS,
OSPF маршрутизирует.

Summarize: TLS связан с TCP/IP, Ethernet, шифрованием, дополняет OSPF.

--------------------------------------------------------------------
9. Итог: целостность в TLS/SSL 🎯🔥
--------------------------------------------------------------------
- **Целостность**: защита от изменений (случайных или злонамеренных).
- **Методы**:
  - Криптографические хэш-функции (SHA-256, SHA-512).
  - Message Authentication Code (MAC) с разделяемым ключом.
- **Процесс**: клиент отправляет данные + MAC, сервер проверяет MAC.
- **Cipher Suite**: включает хэш-функцию для MAC.
- **Ограничение**: не защищает от атак на аутентификацию (Man-in-the-Middle).
- **Популярные алгоритмы**: SHA-256, SHA-512; MD5 и SHA-1 устарели.

**Пример**: В HTTPS MAC (SHA-256 + ключ) защищает данные от подделки
злоумышленником.

Summarize: TLS/SSL используют MAC и SHA для целостности, но требуют PKI
для аутентификации.