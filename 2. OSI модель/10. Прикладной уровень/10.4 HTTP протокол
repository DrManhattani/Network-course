 
 --------------------------------------------------------------------
 Что такое HTTP и откуда он взялся? 🤔
 --------------------------------------------------------------------
 HTTP (HyperText Transfer Protocol) — это протокол передачи гипертекста, лежащий в основе работы 
 системы World Wide Web. 📡
 Его придумал Тим Бернерс-Ли в 1989 году, когда работал в CERN (Европейская организация ядерных исследований). 🚀
 
 Компоненты веба:
   - **HTTP**: протокол для передачи данных.
   - **HTML**: язык разметки гипертекста.
   - **Веб-сервер**: программа, выдающая страницы.
   - **Веб-браузер**: программа для их просмотра.
 
 Первый браузер CERN был текстовым, но вскоре появились графические (например, Mosaic), которые сделали веб популярным. 
 Сегодня Тим Бернерс-Ли — директор W3C (World Wide Web Consortium), который стандартизирует веб-технологии. 🌍
 
 --------------------------------------------------------------------
 HTML: язык гипертекста ✍️
 --------------------------------------------------------------------
 HTML (HyperText Markup Language) — это специальный тип разметки, которую вы добавляете в текстовые документы
 для того, чтобы определить, как показывать ту или иную часть текста. 

 В языке HTML для этих целей используются теги:
   - Используются **теги** для оформления.
   - Примеры:
     - `<h1>Заголовок</h1>` — заголовок.
     - `<ul><li>Элемент списка</li></ul>` — список.
 
 Браузер читает HTML и показывает страницу с заголовками, списками и ссылками. Без разметки — просто текст. 📜
 
 --------------------------------------------------------------------
 URL: адрес в вебе 🌐
 --------------------------------------------------------------------
 URL (Uniform Resource Locator) — это уникальный адрес ресурса в интернете, он же "ссылка". 🔗
 Структура:
   - **Протокол**: `http://` (или `https://`, `ftp://`).
   - **Доменное имя или IP**: `www.asozykin.ru` или `192.168.1.1`.
   - **Путь**: `/courses/networks` (конкретная страница).
 
 Пример:
   - `http://www.asozykin.ru/courses/networks` — страница курса по сетям на моём сайте.
   - URL работает не только с HTTP/HTML, но и с другими протоколами (FTP, HTTPS).
 
 --------------------------------------------------------------------
 Где живёт HTTP? ⚙️
 --------------------------------------------------------------------
 HTTP — это протокол **прикладного уровня** в стеке TCP/IP:
   - Использует **TCP** (транспортный уровень) для надёжной передачи.
   - **Порт сервера**: 80 (стандартный для HTTP).
   - **Порт клиента**: случайный, генерируется ОС (например, 49152).
 
 Модель работы:
   - **Запрос-ответ**: клиент (браузер) запрашивает страницу, сервер отвечает. 📩
 
 --------------------------------------------------------------------
 Версии HTTP 📈
 --------------------------------------------------------------------
 Эволюция протокола:
   - **HTTP/0.9** (1991): экспериментальная версия, только текст, без заголовков.
   - **HTTP/1.0** (1996): первый стандарт, добавлены заголовки.
   - **HTTP/1.1** (1997): улучшенная версия, используется до сих пор.
   - **HTTP/2** (2015): новая версия, ускоряет работу, но пока не везде поддерживается.
 
 Мы разберём HTTP/1.1 — текущий стандарт. HTTP/2 оставим на будущее. ⏳
 
 --------------------------------------------------------------------
 Структура пакета HTTP 📦
 --------------------------------------------------------------------
 HTTP работает в текстовом режиме, без строгого формата (в отличие от DNS). Состоит из трёх частей:
 
 1. **Строка запроса (клиент) или статуса (сервер)**:
    - Запрос: `GET /courses/networks HTTP/1.1` — "Дай страницу".
    - Ответ: `HTTP/1.1 200 OK (код и символьное сообщение)` — "Всё хорошо, вот страница".
 
 2. **Заголовки** (опционально в 1.0, обязательно в 1.1):
	Запрос:
    - `Host: www.asozykin.ru` — домен сервера, у которого вы хотите запросить страницу
	   (сделано из-за того, что на одном и том же IP-адресе
	   может работать несколько веб-сайтов. И веб-серверу необходимо знать, с какого сайта вы
	   хотите загрузить страницу.) 
	Ответ:
    - `Content-Type: text/html; charset=utf-8` — тип передаваемых данных и кодировка.
    - `Content-Length: 5161` — размер сообщения в байтах.
 
 3. **Тело сообщения** (опционально):
    - В запросе: данные для сервера (например, форма). Необязательно, например, в запросе клиента 
	  на передачу веб-страницы с сервера тело не нужно.
    - В ответе: HTML-код запрашиваемой страницы.
 
 После заголовков — пустая строка, затем тело (если есть). 📋
 
 --------------------------------------------------------------------
 Методы HTTP 🛠️
 --------------------------------------------------------------------
 Клиент при обращении к серверу в запросе указывает **метод** — что он хочет сделать:
 
 1. **GET**: получить страницу (самый популярный).
 2. **POST**: отправить данные на сервер (например, комментарий на YouTube).
 3. **HEAD**: взять только заголовки, без тела (проверка страницы).
 4. **PUT**: загрузить ресурс на сервер (нужны права).
 5. **DELETE**: удалить ресурс (нужны права).
 6. **TRACE**: отследить изменения запроса.
 7. **OPTIONS**: узнать, какие методы поддерживает сервер.
 8. **CONNECT**: подключение через прокси.
 
 Чаще всего: GET и POST. Остальные — реже. 🔧
 
 --------------------------------------------------------------------
 Коды статуса HTTP ✅❌
 --------------------------------------------------------------------
 Статусы сгруппированы в 5 групп. Для каждой группы используется код статуса из трёх цифр:
 
 1. **1xx (информационные)**: процесс идёт (редко используется).
 2. **2xx (успех)**:
    - `200 OK`: страница найдена и передана.
 3. **3xx (перенаправление)**:
    - `301 Moved Permanently`: страница переехала навсегда на новый адрес.
    - `307 Temporary Redirect`: временное перенаправление.
 4. **4xx (ошибка клиента)**:
    - `404 Not Found`: страница не найдена.
    - `403 Forbidden`: доступ запрещён.
 5. **5xx (ошибка сервера)**:
    - `500 Internal Server Error`: что-то сломалось на сервере.
 
 --------------------------------------------------------------------
 Пример запроса и ответа 🚀
 --------------------------------------------------------------------
 **Запрос** (вручную через Telnet):
 
 GET /courses/networks HTTP/1.1 			# метод Get для получения ресурса "/courses..." и указываем
											  версию протокола, по которому хотим работать
 Host: www.asozykin.ru						# т.к. версия 1.1, указываем заголовок HOST и доменное имя
											  сервера, с которым мы работаем
 
 - Подключаемся к `www.asozykin.ru:80` по TCP.
 - Просим страницу курса по сетям.
 
 **Ответ**:
 
 HTTP/1.1 200 OK							# статус и версия протокола
 Server: nginx 								# реализация веб-сервера
 Content-Type: text/html; charset=utf-8		# тип передаваемой страницы и кодировка
 Content-Length: 51061						# длина страницы
 
 # также здесь могут идти другие заголовки, которые вам передал сервер
 # далее идет пустая строка и код веб-страницы

 <html>...</html> ``` 

 # после ответа соединение закрывается (или остаётся открытым с заголовком Connection: keep-alive). 🌐
 
 --------------------------------------------------------------------
 Как работает современный веб? 🌟
 --------------------------------------------------------------------
 HTTP создавался для простых страниц в виде запрос-ответ, но сегодня всё сложнее:
 
 • Динамические страницы: страницы генерируются программами, а не лежат на диске.
   Данные берутся из базы данных. 
 • Современные веб-страницы очень сложны, они включают: картинки, скрипты с других серверов
   или программы, которые выполяются на клиенте: JavaScript выполняется в браузере.
 • Сессии: т.е. необходимо отслеживать состояние работы пользователя. Например, корзина в 
   интернет-магазине сохраняется при перемещении между страницами.
 
 --------------------------------------------------------------------
 Итог 🎯
 --------------------------------------------------------------------
 HTTP — это:
 
 • Протокол передачи гипертекста, основа веба. 📜
 • Работает на прикладном уровне через TCP:80. ⚙️
 • Модель запрос-ответ: GET/POST и коды статуса (200, 404). 📩
 • Текстовый формат: строка, заголовки, тело. 📋


