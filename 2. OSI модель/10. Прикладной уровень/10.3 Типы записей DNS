 
 --------------------------------------------------------------------
 Что такое записи DNS? 🤔
 --------------------------------------------------------------------
 DNS (Domain Name System) — это не только про IP-адреса. Это система, 
 решающая множество задач в интернете. 📡

 Каждая запись DNS (по-английски "Resource Record", RR) имеет:
   - **Тип записи**: определяет назначение записи 
	   A - IPv4, 
	   AAAA - IPv6.
   - **Класс записи**: указывает сеть, где она используется.
 
 **Класс**:
   - Сейчас DNS работает только в интернете, поэтому класс всегда 
	 **IN** (Internet). 
   - Другие классы (например, CHAOS) устарели и не применяются. 📜
 
 Типов записей много, и каждый выполняет свою функцию. Давай разберём основные! 🚀
 
 --------------------------------------------------------------------
 Тип A: IP-адрес версии 4 🖧
 --------------------------------------------------------------------
 **Назначение**:
   - Самый распространённый тип — определяет IPv4-адрес по доменному имени.
   - Пример: `www.yandex.ru` → 77.88.55.77.
 
 **Как запросить**:
   - Утилита `nslookup` с ключом `-type=A`.
   - Команда: `nslookup -type=A www.yandex.ru` → список IPv4-адресов.
 
 **Особенность**:
   - Одному имени может соответствовать несколько IP (для распределения нагрузки). 🌍
 
 --------------------------------------------------------------------
 Тип AAAA: IP-адрес версии 6 🌌
 --------------------------------------------------------------------
 **Назначение**:
   - Определяет IPv6-адрес по доменному имени.
   - Пример: `www.yandex.ru` → 2a02:6b8::2:242.
 
 **Как запросить**:
   - `nslookup -type=AAAA www.yandex.ru` → IPv6-адрес.
 
 **Зачем нужен**:
   - IPv6 — будущее интернета, так как IPv4-адресов не хватает. 
   - Используется там, где сети поддерживают новый протокол. 🔮
 
 --------------------------------------------------------------------
 Тип CNAME: псевдонимы (каноническое имя) ✏️
 --------------------------------------------------------------------
 **Назначение**:
   - Создаёт псевдоним (алиас) для доменного имени.
   - Пример: `ftp.asazykin.ru` → `www.asazykin.ru` (оба указывают на один IP).
 
 **Как работает**:
   - CNAME ссылается на другое имя, для которого есть запись A (или AAAA).
   - Удобно: меняешь IP в одной записи A, и все псевдонимы обновляются автоматически.
 
 **Ограничения**:
   - Нельзя создавать цепочки CNAME (например, CNAME → CNAME → CNAME).
   - Подробности в RFC 1034 и RFC 1912. ⚠️
 
 **Альтернатива**:
   - Создать несколько записей A для одного IP. Но это неудобно — при смене IP нужно править все записи. 😓
 
 --------------------------------------------------------------------
 Тип MX: почтовые серверы 📧
 --------------------------------------------------------------------

 Система DNS активно используется при работе электронной почты.
 Но как узнать адрес почтового сервера? - Это можно сделать с помощью DNS. Для этого в нем
 есть запись специального типа MX (Mail Exchange).

 **Назначение**:
   - Указывает серверы, принимающие почту для домена.
   - Пример: для `gmail.com` — несколько MX-записей (mail.google.com и др.).
 
 **Структура состоит из 2 полей**:
   - **Приоритет**: число (меньше = выше приоритет).
   - **Имя сервера**: доменное имя почтового сервера.
   - Пример:
     - `gmail.com` → 10 mail.google.com (приоритет 10).
     - `gmail.com` → 40 alt4.mail.google.com (приоритет 40).
 
 **Как работает**:
   - Почтовый клиент выбирает сервер с наибольшим приоритетом.
   - Если он недоступен, идёт к следующему по приоритету. 🔄
 
 --------------------------------------------------------------------
 Тип SRV: сервисы и порты ⚙️
 --------------------------------------------------------------------

 Для некоторых типов сервисов можно указывать не только IP-адрес, но и порт, на котором этот
 сервис работает. Для этого используется DNS запись типа SRV (Service Record).

 **Назначение**:
   - Указывает компьютер и порт для специфических интернет-сервисов (например, Jabber, SIP).
   - Пример: Jabber-сервер в домене `example.com`.
 
 **Структура запроса**:
   - Формат имени: `_служба._протокол.домен` (например, `_xmpp-server._tcp.example.com`).
   - Поля ответа:
     - **Приоритет**: как у MX (меньше = выше).
     - **Вес**: распределяет нагрузку между серверами с одинаковым приоритетом.
     - **Порт**: номер порта сервиса.
     - **Имя**: доменное имя сервера.
 
 **Пример**:
   - `_xmpp-server._tcp.example.com`:
     - Приоритет 0, вес 5, порт 5222, имя `xmpp.example.com`.
     - Приоритет 20, вес 0, порт 5222, имя `backup.xmpp.example.com`.
 
 **Особенность**:
   - Вес помогает балансировать нагрузку, если приоритеты равны. 🌐
 
 --------------------------------------------------------------------
 Тип NS: серверы зон 📍
 --------------------------------------------------------------------

 Чтобы определять, какие серверы отвечают за ту или иную зону, используются DNS-записи NS
 (Name Server).

 **Назначение**:
   - Указывает DNS-серверы, ответственные за зону (делегирование).
   - Пример:
     - `yandex.ru` → `ns1.yandex.ru`, `ns2.yandex.ru`.
     - `urfu.ru` → `ns1.urfu.ru`, `ns2.urfu.ru`, `ns3.urfu.ru`.
 
 **Где хранится**:
   - На сервере вышестоящей зоны (например, .ru хранит NS для yandex.ru).
 
 **Зачем нужны**:
   - Без NS клиент не узнает, куда обращаться за данными зоны. 🔗
 
 --------------------------------------------------------------------
 Приклеенные записи (Glue Records) 🖇️
 --------------------------------------------------------------------

 Недостаточно знать записи DNS-серверов, нужно еще знать их IP-адреса. Для этого
 используются так называемые прикленные записи A.

 **Назначение**:
   - Указывают IP-адреса серверов NS (A или AAAA).
   - Пример: `ns1.yandex.ru` → 213.180.193.1.
 
 **Почему важны**:
   - NS даёт только имя сервера, но для запроса нужен его IP.
   - "Приклеиваются" к ответу NS, чтобы избежать бесконечных циклов поиска. 🔄
 
 --------------------------------------------------------------------
 Тип PTR: обратные записи 🔙
 --------------------------------------------------------------------

 Помимо определения IP-адреса по доменному имени система DNS может использоваться для
 обратной задачи. Реверсивная зона содержит записи типа PTR (Pointer). Однако из-за 
 технических ограничей DNS не может напрямую работать с IP-адресами, поэтому для
 обратных зон был придуман обходной путь - представлять IP-адрес в виде доменного имени.

 **Назначение**:
   - Определяет доменное имя по IP-адресу (обратный DNS).
   - Используется в специальных **реверсивных зонах**.
 
 **Как работает**:
   - Для IPv4: создан специальный домен `in-addr.arpa`.
   - IP записывается в обратном порядке.
   - Пример: 77.88.55.66 → `66.55.88.77.in-addr.arpa` → `www.yandex.ru`.
 
 --------------------------------------------------------------------
 Как запросить записи с помощью nslookup? 🛠️
 --------------------------------------------------------------------
 **Синтаксис**:
   - `nslookup -type=<тип> <имя>`.
   - Примеры:
     - `nslookup -type=A www.yandex.ru` → IPv4.
     - `nslookup -type=AAAA www.yandex.ru` → IPv6.
     - `nslookup -type=MX gmail.com` → почтовые серверы.
 
 **Полезно**:
   - Можно запросить любой тип записи, зная его название. 🔍
 
 --------------------------------------------------------------------
 Итог 🎯
 --------------------------------------------------------------------
 Типы записей DNS решают разные задачи:
   - **A**: IPv4-адрес по имени. 🖧
   - **AAAA**: IPv6-адрес по имени. 🌌
   - **CNAME**: псевдонимы для имён. ✏️
   - **MX**: почтовые серверы домена. 📧
   - **SRV**: сервисы с портами. ⚙️
   - **NS**: серверы зон (делегирование). 📍
   - **PTR**: имя по IP (обратный DNS). 🔙
 
 Это основные типы, но есть и другие (SOA, TXT и т.д.), которые мы не рассматривали. 


