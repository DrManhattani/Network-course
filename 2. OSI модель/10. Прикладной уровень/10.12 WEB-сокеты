--------------------------------------------------------------------
Введение: что такое вебсокеты? 🌐🔍
--------------------------------------------------------------------
**Вебсокеты** — это протокол прикладного уровня стека
TCP/IP и модели OSI, расположенный на одном уровне с
**HTTP**. Он обеспечивает **двунаправленное постоянное
соединение** между клиентом и сервером, в отличие от
HTTP, который работает по модели "запрос-ответ".
Вебсокеты используют **TCP** на транспортном уровне и
работают на портах **80** (WS) или **443** (WSS с
шифрованием SSL/TLS). Представь вебсокеты как "открытый
телефонный канал" для обмена данными в реальном времени. 😊

**Зачем нужны вебсокеты**:
- HTTP хорош для веб-страниц, но сервер не может сам
  отправить данные клиенту без запроса.
- Вебсокеты решают эту проблему, позволяя обеим сторонам
  обмениваться данными в любой момент. 📡

**Стандарт**:
- Определён в **RFC 6455** (2011).
- Поддерживается всеми современными браузерами
  (Chrome, Firefox, Safari). ✅

**Пример**:
В чате ты отправляешь сообщение. Вебсокеты позволяют
серверу мгновенно разослать его всем участникам, в отличие
от HTTP, где клиенты должны запрашивать обновления. 😎

Summarize: Вебсокеты — протокол прикладного уровня
(RFC 6455) для двунаправленного обмена данными через TCP
(порты 80/443). Решает ограничения HTTP для приложений
реального времени.

--------------------------------------------------------------------
Вебсокеты vs HTTP: в чём разница? 🔄⚖️
--------------------------------------------------------------------
HTTP и вебсокеты решают разные задачи. Давай разберём их
отличия. 😊

**HTTP**:
- **Модель**: запрос-ответ. Клиент шлёт запрос (например,
  GET), сервер отвечает (HTML, JSON).
- **Ограничение**: сервер не может инициировать отправку
  данных, пока клиент не запросит.
- **Применение**: загрузка веб-страниц, API.
- **Недостаток**: для реального времени (чаты, игры)
  требуется частая отправка запросов (polling), что
  нагружает сеть. 😓

**Вебсокеты**:
- **Модель**: постоянное двунаправленное соединение.
  Клиент и сервер могут отправлять данные в любой момент.
- **Применение**: чаты, мессенджеры, стриминг, игры.
- **Преимущество**: низкая нагрузка, мгновенная реакция. 🚀

**Пример**:
В HTTP чат-клиент каждые 2 секунды запрашивает новые
сообщения, тратя трафик. Вебсокеты позволяют серверу
отправить сообщение сразу, как оно появилось. 😎

Summarize: HTTP работает по модели "запрос-ответ" и не
подходит для реального времени. Вебсокеты обеспечивают
постоянное соединение, минимизируя нагрузку.

--------------------------------------------------------------------
Как работают вебсокеты: два этапа 📋⚙️
--------------------------------------------------------------------
Работа вебсокетов делится на два этапа: установление
соединения и передача данных. Давай разберём их. 😊

1. **Установление соединения**:
   - Клиент отправляет **HTTP-запрос** (GET) с заголовками:
     - `Upgrade: websocket` — запрос перехода на вебсокеты.
     - `Sec-WebSocket-Key` — случайный 16-байтный ключ в
       Base64 для защиты от поддельных запросов.
     - `Sec-WebSocket-Version: 13` — текущая версия.
   - Сервер отвечает кодом **101 Switching Protocols**:
     - `Upgrade: websocket`.
     - `Sec-WebSocket-Accept` — ключ, вычисленный из
       клиентского по алгоритму RFC 6455.
   - После этого устанавливается **прямое TCP-соединение**. 📡
2. **Передача данных**:
   - Данные отправляются в **кадрах** (frames) через TCP.
   - Заголовки кадров — бинарные, что снижает накладные
     расходы (в отличие от текстовых заголовков HTTP). ⚡
   - Поддерживается **фрагментация**: большие данные
     разбиваются на несколько кадров.

**Пример**:
Браузер отправляет GET с `Upgrade: websocket`. Сервер
отвечает 101, и чат начинает работать через вебсокеты,
обмениваясь сообщениями без HTTP. 😎

Summarize: Вебсокеты начинаются с HTTP-запроса
(Upgrade, код 101) для установки TCP-соединения. Данные
передаются кадрами с бинарными заголовками.

--------------------------------------------------------------------
Формат кадров вебсокетов: как устроены? 📊🔢
--------------------------------------------------------------------
Данные в вебсокетах передаются **кадрами**. Давай разберём
их структуру. 😊

**Типы кадров**:
1. **Текстовые**: данные в UTF-8 (например, JSON для чата).
2. **Бинарные**: любые данные (файлы, изображения).
3. **Управляющие**:
   - **Ping/Pong**: проверка соединения.
   - **Close**: разрыв соединения. 📋

**Структура заголовка кадра**:
- **Флаги** (первые биты):
  - **FIN**: 1 = последний кадр (нет фрагментации); 0 =
    часть сообщения.
  - **RSV1–RSV3**: зарезервированы для расширений.
  - **MASK**: 1 = данные маскированы (обязательно для
    клиента).
- **Opcode** (4 бита): тип кадра (текст = 1, бинарный = 2,
  Ping = 9, Pong = 10, Close = 8).
- **Payload Length**: длина данных:
  - 7 бит: до 125 байт.
  - 16 бит: до 65 535 байт.
  - 64 бита: большие данные.
- **Masking Key** (4 байта, если MASK = 1): случайный ключ
  для XOR-маскирования данных (защита от атак).
- **Payload Data**: сами данные (маскированные, если
  MASK = 1). 🔢

**Пример**:
Клиент отправляет текстовое сообщение "Привет" (UTF-8).
Кадр: FIN = 1 (целиком), Opcode = 1 (текст), MASK = 1,
Masking Key = случайный, Payload = "Привет" (XOR с ключом).
Сервер отвечает кадром Pong для проверки соединения. 😎

Summarize: Кадры вебсокетов — текстовые, бинарные или
управляющие (Ping/Pong, Close). Заголовок включает флаги
(FIN, MASK), Opcode, длину и ключ маскирования.

--------------------------------------------------------------------
Ping/Pong и Close: поддержание и разрыв соединения 📡🛑
--------------------------------------------------------------------
Вебсокеты используют управляющие кадры для управления
соединением. Давай разберём их. 😊

**Ping/Pong**:
- Сервер шлёт **Ping** (Opcode = 9) клиенту периодически.
- Клиент отвечает **Pong** (Opcode = 10).
- **Задачи**:
  - Проверка активности (живо ли соединение?).
  - Поддержание TCP-соединения через прокси или
    балансировщики. 📡
- Периодичность зависит от реализации (например, каждые
  30 сек).

**Close**:
- Любая сторона может отправить кадр **Close**
  (Opcode = 8) для разрыва.
- Сторона, получившая Close, отвечает Close и закрывает
  TCP-соединение. 🛑

**Пример**:
Сервер шлёт Ping каждые 30 сек. Клиент отвечает Pong,
поддерживая соединение. Если клиент хочет выйти из чата,
он шлёт Close, сервер подтверждает, и соединение
закрывается. 😎

Summarize: Ping/Pong проверяют активность и поддерживают
TCP через прокси. Close разрывает соединение, инициируемое
любой стороной.

--------------------------------------------------------------------
Маскирование данных: зачем и как? 🔐⚙️
--------------------------------------------------------------------
Вебсокеты используют **маскирование** для безопасности.
Давай разберём, как это работает. 😊

**Зачем маскирование**:
- Защищает от атак через прокси (например, кэширование
  вредоносных данных).
- Клиент **обязательно** маскирует данные (MASK = 1).
- Сервер маскирует опционально (обычно нет). 🔐

**Как работает**:
- Клиент генерирует случайный **4-байтный ключ
  маскирования** для каждого кадра.
- Данные в Payload XOR-ятся с ключом:
  - Пример: данные = [0x41], ключ = [0xFF], результат =
    [0xBE].
- Получатель использует тот же ключ для декодирования
  (повторный XOR). ⚙️

**Пример**:
Клиент шлёт "Hi" (UTF-8). Кадр: MASK = 1, ключ =
[0x1A, 0x2B, 0x3C, 0x4D], данные XOR-ятся с ключом.
Сервер декодирует, применяя тот же ключ. 😎

Summarize: Маскирование (обязательное для клиента)
защищает данные через XOR с 4-байтным случайным ключом,
предотвращая атаки через прокси.

--------------------------------------------------------------------
Преимущества вебсокетов: почему они лучше? 🚀🌟
--------------------------------------------------------------------
Вебсокеты имеют ряд преимуществ перед HTTP для приложений
реального времени. Давай разберём их. 😊

- **Двунаправленность**: клиент и сервер могут отправлять
  данные в любой момент, без ожидания запросов. 📡
- **Низкие накладные расходы**: бинарные заголовки кадров
  (2–14 байт) против текстовых заголовков HTTP (сотни
  байт). ⚡
- **Эффективность**: нет необходимости в частых запросах
  (polling), как в HTTP, что снижает нагрузку на сеть.
- **Совместимость**: работают на портах 80/443, интегрируются
  с инфраструктурой HTTP (прокси, балансировщики). ✅
- **Гибкость**: поддерживают фрагментацию, текстовые и
  бинарные данные, расширения. 📊

**Пример**:
В игре вебсокеты обновляют позиции игроков мгновенно, без
задержек polling’а HTTP, экономя трафик и ресурсы. 😎

Summarize: Вебсокеты быстрее и эффективнее HTTP благодаря
двунаправленности, бинарным заголовкам и совместимости с
веб-инфраструктурой.

--------------------------------------------------------------------
Связь с курсом: вебсокеты в контексте 📚🔗
--------------------------------------------------------------------
Вебсокеты — важная часть курса, связанная с другими темами.
Давай разберём, как. 😊

- **HTTP**: Вебсокеты дополняют HTTP, используя его для
  установки соединения, но заменяют для реального времени
  (лекция про HTTP). 🌐
- **TCP**: Вебсокеты опираются на надёжное TCP-соединение,
  в отличие от UDP (лекция про транспортный уровень). 📡
- **Прикладной уровень**: Вебсокеты, как HTTP, работают на
  уровне приложений, взаимодействуя с браузерами и серверами
  (лекция про OSI/TCP/IP). 🔄
- **SSL/TLS**: WSS использует шифрование, как HTTPS
  (лекция про безопасность). 🔐

**Пример**:
В мессенджере вебсокеты (WSS) передают сообщения через
TCP на порту 443, используя HTTPS-инфраструктуру и
шифрование TLS. 😎

Summarize: Вебсокеты связаны с HTTP, TCP, SSL/TLS и
прикладным уровнем. Дополняют HTTP для реального времени,
используя существующую инфраструктуру.

--------------------------------------------------------------------
Итог: зачем нужны вебсокеты? 🎯🔥
--------------------------------------------------------------------
Вебсокеты — мощный инструмент для современных приложений.
Давай подведём итоги. 😊

- **Суть**: протокол прикладного уровня (RFC 6455) для
  двунаправленного обмена через TCP (порты 80/443). 🌐
- **Этапы**: установка соединения (HTTP Upgrade, код 101)
  и передача данных (кадры). 📡
- **Кадры**: текстовые, бинарные, управляющие
  (Ping/Pong, Close) с бинарными заголовками. 📊
- **Особенности**: фрагментация, маскирование (XOR),
  низкие накладные расходы. ⚙️
- **Преимущества**: быстрота, эффективность,
  совместимость с HTTP-инфраструктурой. 🚀
- **Применение**: чаты, игры, стриминг, IoT. 🎮

**Пример**:
В WhatsApp вебсокеты доставляют сообщения мгновенно,
экономя трафик и работая через стандартные порты, что
идеально для мобильных сетей. 😎

Summarize: Вебсокеты — эффективный протокол для реального
времени, дополняющий HTTP. Обеспечивают двунаправленность,
низкие расходы и совместимость с веб.