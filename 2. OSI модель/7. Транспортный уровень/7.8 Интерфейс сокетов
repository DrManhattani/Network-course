		
" ==============================================================================
" 🌐 ИНТЕРФЕЙС СОКЕТОВ: общие сведения
" ==============================================================================
  Интерфейс сокетов — это де-факто стандарт взаимодействия прикладной
  программы с ядром ОС — точка входа в сеть для приложения. Он соединяет
  прикладной уровень стека TCP/IP, который реализуется в пользовательском
  пространстве, с нижним уровнями, которые, как правило, реализуются в ядре
  ОС.

  Транспортный уровень - это первый уровень, с которым может взаимодействовать
  программист. Поэтому интерфейс транспортного уровня, который позволяет писать
  программы для сети, надо рассматривать.

  ГЛОБАЛЬНО:
	📌 Интерфейсы нужны для взаимодействия между разными уровнями модели OSI.
	   Каждый уровень OSI выполняет свои задачи и не "знает", как работают 
	   другие уровни. Поэтому между уровнями есть чёткие интерфейсы — они позволяют 
	   уровням обмениваться данными и скрывать детали реализации.

	📌 Проще говоря:
	🔹 Сокет — это программный "разъём", через который две программы могут 
	   общаться по сети.
	🔹 Интерфейс сокетов (Sockets API) — это набор функций, с помощью которых 
	   программа может работать с сокетами.

" -----------------------------------------------------------------------------
" 🤔 Что такое сокеты? Общие сведения
" -----------------------------------------------------------------------------
  📅 Сокеты впервые появились в Berkeley UNIX (BSD) в 1983 году. В этой системе 
	 сокеты были реализованы как файлы специального типа, что позволяло работать 
	 с сетью так же, как с обычными файлами:
  	 	• Запись в сокет → данные отправляются по сети.
  	 	• Чтение из сокета → данные приходят из сети.
		* передача данных по сети скрыта от программиста

  Де-факто сокеты - это стандарт для взаимодействия приложений с транспортным
  уровнем стека протоколов TCP/IP.

  Эта абстракция упрощает жизнь: вы не думаете о трёхкратном рукопожатии TCP или
  потерянных пакетах UDP. Сокеты стали стандартом:
    - UNIX, Linux, Windows.
    - C, Python, Java и другие языки.
    - Де-факто способ писать сетевые программы. 🚀

" -----------------------------------------------------------------------------
" 🔌 Зачем нужны сокеты?
" -----------------------------------------------------------------------------
  Сокеты связывают ваше приложение с транспортным уровнем:
    - Создавайте клиент-серверные программы.
    - Работайте с TCP/UDP без низкоуровневых деталей.
  Если протоколы изменятся, ваш код останется рабочим — это сила интерфейса.

  Поскольку сокеты — это, фактически, интерфейс для погружения на третий
  уровень TCP/IP-стека, сокеты не предоставляют механизмов для управления
  кодированием данных и сенсами работы приложений — они просто позволяют
  передать "сырой" поток байт. 

" -----------------------------------------------------------------------------
" ⚙️ Основные операции с сокетами Беркли
" -----------------------------------------------------------------------------
  Сокеты — это набор функций для управления сетью. 
  Большая часть современных сокетов имеет такие же операции, как и в сокетах
  Беркли, либо похожие на них.

  I. СОЗДАНИЕ СОКЕТОВ:

  	1. Создание сокета
     	Функция: socket()
     	Действие: Создаёт сокет — точку входа в сеть.
     	Параметры: IPv4/IPv6 + TCP/UDP.
     	Пример: socket(AF_INET, SOCK_STREAM) → TCP/IPv4.
				Т.е. создается объект сокет, в простейшем случае это просто
				 файл специального вида.

  	2. Привязка (Bind)
     	Функция: bind()
     	Действие: Связывает сокет с IP и портом.
     	Пример: bind(("0.0.0.0", 80)) → слушает все интерфейсы, порт 80.
     	Клиент: Пропускает, ОС сама выбирает порт.
				Для клиента, как правило, не имеет значения какой IP и порт
				 используются. Номер порта назначается ОС, поэтому метод bind
				 на клиентском сокете обычно не вызывается. Хотя это можно
				 сделать.


  	3. Прослушивание (Listen)
     	Функция: listen()
     	Действие: Ждёт входящие подключения, задаёт очередь. В вызове
				  необходимо указать размер этой очереди.
     	Пример: listen(5) → очередь на 5 клиентов. 
				Говорит о том, что сокет готов принимать соединения по сети.
				Если сервер получит больше, чем 5 запросов на соединение, 
				 а предыдущие вызовы еще не будут обработаны, то новые запросы 
				 отбрасываются.
				У клиента, при подключении к серверу, как правило, не вызывается.

  II. УСТАНОВКА СОЕДИНЕНИЯ:

  	4. Принятие (Accept)
     	Функция: accept()
     	Действие: Создаёт новый сокет для клиента, исходный продолжает слушать.
				  Т.е. при поступления запроса на сервер от клиента на сервере,
				   для того, чтобы другие клиенты сервера могли соединяться
				   с этим сервером на этом же IP-адресе и на этом же порту,
				   создается копия сокета. И данные уже передаются через копию	
				   сокета.
 				   
				  Сервер ждет подключения с помощью метода accept.
     	Результат: Новый сокет + IP клиента.

  	5. Соединение (Connect)
     	Функция: connect()
     	Действие: Клиент подключается к серверу.
     	Пример: connect(("192.168.1.1", 80)).
				В параметрах метода указывается IP-адрес и порт сервера, с которым
				 нужно установить соединение.

  III. ПЕРЕДАЧА ДАННЫХ:

  	6. Передача данных
     	Функции: send(), recv()
     	Действие: Обмен данными (send — отправить, recv — принять).
     	Пример: send("Hi"), recv(1024).
				Клиент после того, как сервер создал копию сокета, т.е. после того,
				 как установил соединение с сервером, вызывает метод send, чтобы
				 передать порцию данных.
				Данные передаются по сети, а сервер их может прочитать с помощью
				 метода receive.

  IV. ЗАКРЫТИЕ СОЕДИНЕНИЯ:

  	7. Закрытие
     	Функция: close()
     	Действие: Завершает соединение.
				  После того, как все данные переданы, клиент вызывает метод close.
				  После чего происходит разрыв соединения.

  🖧  СЕРВЕР вызывал методы:
	• socket - создается объект сокет
	• bind - сокет присоединяется к IP-адресу и порту
	• listen - создется размер очереди на соединение
	• accept - сервер готов принимать соединения и он переходит в режим пассивного
			   ожидания, ждет запросов на установку соединения от клиента. 
			   (После запроса на соединение от клиента создается копия этого сокета)
	• receive - читает данные с помощью метода.

  💻 КЛИЕНТ вызывал методы:
	• socket - для создания сокета
	• connect - в параметрах метода указывается IP-адрес и порт, с которыми нужно
				установить соединение. Отправляется запрос на соединение.
	• send - подготавливается порция данных, вызывается метод, данные передаются
			 по сети.
	• close - разрывает соединение.

" -----------------------------------------------------------------------------
" 🐍 Пример на Python: Сервер и Клиент
" -----------------------------------------------------------------------------

🖧  Сервер
import socket 
			  # импортируем модуль сокет
server_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM) 
			  # создаем сокет
			  # задаем протокол сетевого уровня, здесь указан IPv4
			  # задаем протокол транспортного уровня, здесь указан TCP
server_socket.bind(("192.168.0.1", 8888))
			  # вызываем метод, привязываем его к IP-адресу и порту
server_socket.listen(1)
			  # говорим о том, что готовы принимать соединения и очередь
				будет состоять всего из одного соединения.
client_socket, addr = server_socket.accept()
			  # вызываем метод accept, ждем запросов от клиента на соединение.
			  # возврат из метода произойдет только после того, как какой-то
				из клиентов установит соединение.
while True:
    data = client_socket.recv(1024)  # Читаем данные по 1024 байта
    if not data:
        break						 # выход из цикла происходит, когда данные
									   закончатся
    client_socket.sendall(data)      # Эхо: отправляем те же самые данные обратно
client_socket.close()				 # закрываем соединение

💻 Клиент
import socket
			  # импортируем модуль сокет

client_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
			  # создаем сокет
			  # задаем протокол сетевого уровня, здесь указан IPv4
			  # задаем протокол транспортного уровня, здесь указан TCP

client_socket.connect(("192.168.0.1", 8888))       
			  # Подключаемся
			  # не вызывается метод bind для присоединения к IP-адресу и порту,
				это все делается автоматически библиотекой Python и ОС
client_socket.send("Привет, мир!".encode())      
			  # Отправляем серверу сообщение
data = client_socket.recv(1024)                  
			  # Принимаем от него данные, которые будут те же самые
print(f"Ответ: {data.decode()} 📩")
client_socket.close()
			  # закрываем соединение

" -----------------------------------------------------------------------------
" 📡 Типы сокетов и протоколы
" -----------------------------------------------------------------------------
  Сокеты определяются протоколами:
    Сетевой уровень:
      - AF_INET: IPv4 ("192.168.1.1").
      - AF_INET6: IPv6 ("2001:db8::1").
    Транспортный уровень:
      - SOCK_STREAM: TCP — надёжный поток.
      - SOCK_DGRAM: UDP — быстрые датаграммы.
      - SOCK_RAW: Низкий уровень (редко).

  TCP vs UDP:
    - TCP: Доставка гарантирована, порядок сохранён (веб, чаты).
    - UDP: Быстрее, но без гарантий (игры, стриминг).

" -----------------------------------------------------------------------------
" 🎯 Вывод
" -----------------------------------------------------------------------------

  • Сокеты нужны, чтобы писать программы для сетей, которые взаимодействуют напрямую
   с транспортным уровнем. Хотя сейчас многие программы взаимодействуют не с
   транспортным, а с прикладным уровнем, который предоставляет больше возможностей.
  • Протоколы транспортного уровня скрыты от программиста.


