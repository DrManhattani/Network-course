

" ==============================================================================
" 🌐 БАЗОВЫЕ ПОНЯТИЯ 
" ==============================================================================

  Сети строятся в виде нескольких уровней, организованные один над другим, и
  каждый уровень предоставляет сервис вышестоящему уровню.

  * Сервис - описывает какие функции реализует уровень (что полезного делает
	уровень).
  * Интерфейс - находится внутри одного компьютера и задает набор примитивных 
	операций, который верхний уровень должен вызвать для того, чтобы
	воспользоваться сервисом нижестоящего уровня.
  * Протокол - правила и соглашения, которые используются уровнями разных хостов
	для взаимодействия друг с другом по сети.  


" ==============================================================================
" 🌐 ТРАНСПОРТНЫЙ УРОВЕНЬ TCP/IP
" ==============================================================================

  Интерфейс - интерфейс сокетов, с использованием которых пишут программы,
			  которые взаимодействуют с сетью на транспортном уровне.
  Протоколы - TCP, UDP

" ==============================================================================
" 🌐 ЗАЧЕМ РАЗДЕЛЯТЬ ПОНЯТИЕ ПРОТОКОЛА И ИНТЕРФЕЙСА?
" ==============================================================================

  • Изоляция решений - общий принцип проектирования: описание и реализация 
	должны быть отделены друг от друга. 

	На транспортном уровне есть 2 вида изоляции:
	
	- ВНУТРИ КОМПЬЮТЕРА - когда работаем внутри одного компьютера, т.е. пишем
	   программы, которые будут взаимодействовать с сетью, мы используем
	   интерфейсы.
	  На транспортном уровне - это интерфейс сокетов.
	  Реализация сетевого взаимодействия, которая выполнена в протоколах, скрыта
	   от нас. Т.о., если протоколы изменятся, нам не придется менять нашу
	   программу.

	- МЕЖДУ КОМПЬЮТЕРАМИ - постоянными сохраняются протоколы, а интерфейсы и
	   программы, которые находится внутри компьютера, скрыты от других
	   компьютеров и устройств. За счет этого обеспечивается возможность
	   взаимодействия по сети устройств, использующих разные ОС, т.к. они
	   используют один и тот же набор протоколов TCP/IP.

" ==============================================================================
" 🌐 ЗАЧЕМ РАЗДЕЛЯТЬ ПОНЯТИЕ СЕРВИС И ИНТЕРФЕЙСА?
" ==============================================================================

  Сервис - это абстрактное описание того, что делает уровень. Оно описывает,
		   что бы мы хотели, чтобы он делал, не привязываясь ни к каким конкретным
		   деталям реализации. 

	Например, стек протоколов TCP/IP предоставляет на транспортном уровне 2 типа
	сервиса:
	- надежная передача потока байт
	- ненадежная передача коротких сообщений

  Интерфейс - набор операций для доступа к сервису.

	Например, стек протоколов TCP/IP предоставляет на транспортном уровне
	интерфейсы:
	- сокеты
	- через этот интерфейс мы можем получить доступ к сервисам двух типов: надежной
	  и ненадежной доставке данных.

