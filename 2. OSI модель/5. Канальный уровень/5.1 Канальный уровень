
	✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤ КАНАЛЬНЫЙ УРОВЕНЬ ✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤✤

	(Data Link layer) - второй уровень семиуровневой системы модели OSI. Он отвечает за 
	передачу кадров через физический канал в пределах локальной сети, обеспечивает 
	обнаружение и коррекция ошибок, а также управление доступом к среде передачи и отвечает
	за адресацию в пределах локальной сети. 	
	
	✅ ОСНОВНЫЕ ЗАДАЧИ ✅
		1. Разделение данных на кадры:
			-- информация, поступающая с сетевого уровня, разделяется
			на кадры: небольшие блоки данных, снабженные заголовком и контрольной суммой.
		2.  Обнаружение и исправление ошибок:
			-- проверяет целостность передаваемых данных с помощью контрольных сумм (наример,
			CRC - Cyclic Redundancy Check, проверка циклического резервирования).
			-- если обнаружена ошибка, данные могут быть повторно запрошены.
		3. Управление доступом к среде передачи:
			-- в сетях с общей средой передачи (например, Wi-Fi или Ethernet) канальный уровень
			регулирует, кто и когда может передавать данные, чтобы избежать коллизий.
			-- используются протоколы, такие как CSMA/CD (в Ethernet) или CSMA/CA (в Wi-Fi).
		4. Адресация на уровне узлов:
			-- на канальном уровне используется MAC-адресация (Media Access Control). Каждый
			сетевой интерфейс имеет уникальный MAC-адрес, который используется для идентификации
			устройств в пределах локальной сети (LAN).


	🎯 КАДРЫ КАНАЛЬНОГО УРОВНЯ 

		🟢 ОБЩИЙ ВИД
		• Заголовок (Header): включает информацию, такую как адреса отправителя и получателя 
			(src/dst MAC-адреса), тип протокола (E-Type) и другие параметры.
		• Поле данных (Payload): содержит непосредственно передаваемые данные. Это может быть часть сетевого 
			пакета, переданного с вышестоящих уровней модели OSI.
		• Контрольная последовательность (Trailer или CRC): используется для проверки целостности данных. 
			CRC (Cyclic Redundancy Check) — это контрольная сумма, которая помогает обнаружить ошибки, 
			возникшие при передаче данных.	
	

		📌 МЕТОДЫ РАЗДЕЛЕНИЯ КАДРОВ 

		🟡 1. Указание длины кадра - чтобы разделять кадры, в начале каждого кадра указывается
	          его длина (количество байт) - НО ДАННЫЕ МОГУТ ИСКАЖАТЬСЯ ПРИ ПЕРЕДАЧЕ. Если произошло хотя
		      бы одна ошибка, то вся последовательность кадров будет нарушена - ПОЭТОМУ НА ПРАКТИКЕ ЭТОТ
		      МЕТОД НЕ ПРИМЕНЯЕТСЯ!
		🟡 2. Использование специальных последовательностей -  в начало и конец кадров вставлять специальные
		      последовательности байтов или бит.
			📝2.1 протокол BSC - текстовые символы: DLE STX (начало кадра), DLE ETX (конец кадра).
			      Start/end of Text. ПРОБЛЕМА в том, что в данных ТОЖЕ может встретиться ТАКАЯ последовательность.
			      Для того, чтобы отличать последовательность, которая используется в данных от управляющих
			      символов используется Escape последовательность в данных DLE (data link escape).
			      Протокол изобрели IBM. Он считается УСТАРЕВШИМ.
			📝2.2 протокол HDLC и PPP - добавляются биты:
				  - в начало и конец кадра добавляется - 01111110
				  - чтобы решить проблему конфликта данных с управляющими символами, после каждых пяти идущих
			      подряд единиц добавляется 0, который игнорируется получателем.
			      Протокол используется очень РЕДКО. В точках с прямым подключением (point-to-point) и в старых
			      WAN сетях (wide area network).
			🌐2.3 ETHERNET - технология и набор протоколов, которые работают на канальном уровне (Data Link 
			      Layer) модели OSI. Ethernet определяет стандарты для передачи данных в локальных сетях (LAN) 
			      и включает в себя как физический уровень (Physical Layer), так и канальный уровень.
			
				🖧  СТРУКТУРА ETHERNET-КАДРА:
				a) Преамбула (preamble) - использовалась в классическом Ethernet. Это такая последовательность
				   данных, которая передается перед началом каждого кадра. Она состоит из 8 байт. 
				   Первые 7 байт представляют собой чередующиеся 10101010. Последний байт 10101011.
				   В более поздних версиях Ethernet (в частности, Fast Ethernet) другая схема кодирования:
				   Используются символы, которые не используются для представления данных в качестве
				   сигналов о начале и конце кадра. Перед отправкой передаются символы J и K. После
				   окончанию символ T. 
				б) MAC-адрес source, 6 байт
				в) MAC-адрес destination, 6 байт
				г) Type/Lenght - тип протокола и длина данных, 2 байта (IPv4-0800, IPv6-86DD, ARP-0806).
				д) Данные (Data) - 46-1500 байт.
				ж) Контрольная сумма (Frame Check Sequence) - алгоритм для обнаружения ошибок. Если 
				   произошла ошибка - кадр отбрасывается и производится повторная отправка (но есть коды, 
				   исправляющие ошибки), 4 байта.
					🔷 МЕТОДЫ ПОВТОРНОЙ ОТРАВКИ:
					1. Остановка и ожидание - отправляется кадр и ждется подтверждение о его получении.
						🔹Используется в технологии канального уровня Wi-Fi.
					2. Скользящее окно - отправляется сразу несколько кадров. 
						🔹Количество сообщений, которое можно передать, не дожидаясь подтверждения 
						  называется РАЗМЕРОМ ОКНА.
						🔹Получатель подтверждает сразу получение порции кадров.
						🔹На канальном уровне не используется. Используется на транспортном уровне TCP. 
				🔸Каналы с редкими ошибками (когда идет передача по проводам) -  просто обнаруживают ошибки
				  на канальном уровне.
				🔸Каналы с частыми ошибки (Wi-Fi) - занимаются повторной отправкой сразу на канальном уровне.		
			
		📌 ПОДУРОВНИ КАНАЛЬНОГО УРОВНЯ:

		🟡 Модель OSI разрабатывалась для каналов связи точка-точка - последовательные линии связи, которые
		   объединяли большие компьютеры.
		🟡 Когда появились разделяемые каналы связи (когда к одной и той же среде передачи данных подключено
		   несколько устройств), модель пришлось изменить.

			ПОДУРОВНИ:
			🔶 Управления логическим каналом (Logical Link Control, LLC)
				🔸отвечает за передачу данных (создание кадров, обработка ошибок и т.п.)
				🔸общий для разных технологий канального уровня. 
				🔸мультиплексирование - передача данных для нескольких протоколов вышестоящего уровня
				 (IP, ARP, ICMP) на уровне MAC.
				🔸управление потоком - предотвращение затопления медленного устройства быстрым отправителем.
			🔶 Управление доступом к среде (Media Access Control, MAC). 
				🔸используется, если технология канального уровня - с разделяемой средой передачи данных.
					🔸обеспечивает согласованный доступ в каналах связи с разделяемой средой передачи данных. 
					🔸если есть несколько устройств, подключенных к одному и тому же каналу связи, то при
				  	  передаче данных мы должны указать, к какому устройству эти данные предназначены.
				  	  Для этого используется адресация.
				🔸если точка-точка - подуровень необязателен.

				🔷 Множественный доступ - зачем обеспечивать:
					🔹предотвращать коллизии - искажение данных, если несколько компьютеров передают
					  одновременно. Поэтому в один момент времени канал связи должен использовать
					  только один отправитель.
				🔷 Методы управления доступом
					🔹рандомизированный - вероятность выбора 1/N, где N - количество подключенных
					  устройств (Ethernet, Wi-Fi).
					🔹на основе правил использования (Token Ring).	
